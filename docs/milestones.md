# V4 Milestones

## Goal

- 在新目录 `pdf-rag-agent-v4` 中落地 Workflow-first 研究型 Agent V4。
- 验收以 `evals/cases_test_md.yaml` 为准，完整覆盖 `docs/test.md` 的所有问题。
- 目标延迟优先压到单 turn `<120s`，硬上限 `<180s`。

## Locked Decisions

- 新项目独立目录，不覆盖 V2/V3。
- 首版交付为 `API + SSE + benchmark/eval + 里程碑文档`。
- ingestion 明确重做，建立 `paper index + block index` 双索引。
- 摘要策略为“Zotero 原生摘要优先，缺失时 LLM 补全”。
- `conversation` 问题零检索，precision task 必须引用证据。
- ingestion 过滤 `tag=书籍`，并仅保留论文型条目或论文型网页来源。
- 向量入库增加 timeout / retry / resume，避免最后批次卡死后整轮重来。
- 概念/术语类问题新增独立 `concept_definition` 主干，不再复用普通 paper-summary 路径。
- 对 target-bearing 的通用问题新增 relevance guardrail：找不到稳定证据时澄清，而不是返回弱相关论文摘要。
- 运行时配置优先级固定为 `环境变量 > V4 .env > 旧仓库 .env > 默认值`，避免 V4 被旧项目的 `EMBEDDING_MODEL=text-embedding-3-small` 反向覆盖。
- V4 当前固定使用 `EMBEDDING_MODEL=text-embedding-3-large`，与现有 `zprag_v4_*` collection 的向量维度保持一致。
- 研究类最终回答不再使用硬编码 fallback 模板；线上主路径固定为 `claims/evidence -> LLM final composer -> Markdown answer`，若 LLM 不可用则直接报错。
- V4 前端默认走 `/api/v1/v4/chat/stream`，显式展示 workflow 节点进度，并将最终结果按 Markdown 渲染。

## Current Risks

- Zotero `abstractNote` 在不同库里的覆盖率可能不稳定，需要 fallback summary。
- `--resume` 目前只在向量入库阶段续跑，若前置 PDF 抽取中断仍会重新抽取。
- 通用概念题目前已具备 acronym expansion + safe clarify，但更复杂的开放式百科问答仍需后续继续扩充 intent/router。
- 线上仍有少量旧页面在请求 `/query`，需要和 V4 前端调试结果区分开，不应混淆为当前 `/api/v1/v4/chat` 的行为。
- 前端现在默认采用 `Ctrl/Cmd+Enter` 发送、`Enter` 换行，以避免中文输入法下的回车误发；如后续要支持可配置快捷键，需要额外产品层决策。

## Milestone Status

- `M0`: completed
- `M1`: completed
- `M2`: completed
- `M3`: completed
- `M4`: completed
- `M5`: completed

## Latest Benchmark

- 2026-04-23 本地完整入库后 benchmark：`9 / 9` case 通过。
- 最新 ingest 结果：`paper_records=116`，`papers_indexed=114`，`paper_docs=114`，`block_docs=11128`，`vectors_upserted=11242`，`papers_with_generated_summary=3`。
- 最新 eval 结果：`avg_latency_ms=1944.86`，`p95_latency_ms=5137.11`。
- 2026-04-23 新增通用概念 smoke check：`PPO / RLHF / DPO / GRPO / RAG` 可走 `concept_definition`，未知术语会返回澄清而非随机摘要。
- 2026-04-23 修复 V4 配置覆盖链路后，线上服务已按 `text-embedding-3-large` 重启；本地 smoke check 中 `你是谁 / AlignX是什么 / DeepSeek R1 Figure 1` 均返回正确路由与正常回答。
- 2026-04-23 新的流式前端已接入真实 SSE 事件链，stream smoke check 已确认可依次收到 `session -> contract -> plan -> candidate_papers -> screened_papers -> evidence -> claims -> verification -> answer_delta -> final`。
- 2026-04-23 V4 运行时 `CHAT_MODEL` 已切换为 `gpt-4o` 并重启生效，用于优先测试更快的在线回复速度。
- 2026-04-26 收口本地 PDF preview 访问控制、active paper 指代绑定、paper scope 纠错继承和多目标 follow-up 保留；本地全量 `pytest -q` 通过 133 个测试。
- 2026-04-26 加硬 `topic_state` 判别 prompt，强化 `target_alias -> body_acronyms` 召回加权，要求多论文 claims 按论文分节输出，并删除冗余 training component normalize；本地全量 `pytest -q` 通过 135 个测试。
- 2026-04-26 前端 UI refresh：顶部叙事改为 structured intent / active memory / grounded multimodal RAG；聊天区新增 runtime pipeline；Run 面板新增 Intent / Memory / Evidence / Verify 结构图，右侧 summary 改为 Intent / Tools / Grounding。
- 2026-04-26 前端 Markdown/LaTeX 渲染加固：数学公式在进入 Markdown parser 前用私有占位符保护；KaTeX 未加载时不再泄漏占位符，而是显示可读 fallback，覆盖 DPO 这类长公式场景。
- 2026-04-26 公式输出 prompt 收口：公式抽取器要求产出可直接放入 `$$...$$` 的标准 LaTeX，变量 symbol 也要求可放入 `$...$`；新增通用数学符号规范化，将 `∇θLDPO / πθ / πref / logσ` 等紧凑 OCR/LLM 输出转成 KaTeX 友好的 LaTeX。
- 2026-04-26 对话体验收口：`怎么理解这个公式` 走上一轮公式记忆解释，不再重复检索；`不是这个公式` 保留上一轮论文范围并偏向重新找目标函数/损失；`我要中文` 作为回答语言修正处理，不再触发随机论文检索。前端新会话现在会清空聊天流、预览区和 runtime 状态。本地全量 `pytest -q` 通过 139 个测试。
- 2026-04-27 运行时检索增加历史书籍过滤，避免已入库的书籍/教程污染论文 origin 问题；origin 类召回新增“提出/引入目标词”排序信号，`Transformer` 起源真实库 smoke check 中《Attention Is All You Need》排第一。PDF 预览允许无 key 的同源浏览器页面访问但仍阻断裸远程请求。前端改为更现代的三栏 Agent 工作台，新增夜间模式切换。本地全量 `pytest -q` 通过 141 个测试。
- 2026-04-27 前端重新设计为 GPT/DeepSeek 风格的聊天主界面：左侧抽屉/侧栏承载本地历史会话与论文库切换，中间只保留主聊天和输入框，右侧承载 Evidence / PDF / Run 三个 inspector 面板；浅色/夜间模式改为单套 token 驱动，兼容桌面三栏与移动端抽屉布局。本地全量 `pytest -q` 通过 141 个测试。
- 2026-04-27 前端交互修补：回答完成后强制隐藏进度圈；恢复 clarification options 点击选择；用户消息右对齐以区别助手；会话历史支持本地删除；SSE `answer_delta` 兼容 `text/delta/content` 字段。`/chat/stream` smoke check 已确认 `session -> contract -> answer_delta -> final` 事件链正常，本地全量 `pytest -q` 通过 141 个测试。
- 2026-04-27 AlignX 路由收口：显式目标的“主要结论/数据支持”问题不再被 LLM 扩写到 `AlignXplore`；“第一个/第一篇/首次提出 X”本地保护为 `origin_lookup`，并禁止 origin 查询继承陈旧 `selected_paper_id`。paper summary solver 优先精确命中或引入目标的论文。前端移除输入栏 `Transformer/DPO` 固定快捷按钮；线上服务已重启，smoke check 中 `AlignX中主要结论...` 和 `我问的是AlignX的第一篇论文` 均命中《From 1,000,000 Users to Every User...》。本地全量 `pytest -q` 通过 145 个测试。
- 2026-04-28 按 `docs/review-2026-04-28.md` 推进 M1/M2：现有 Agent 工具补齐真实 `input_schema`，`ModelClients._openai_tool_definitions` 改为透传 schema，不再把所有工具参数强制压成 `{reason}`；runtime 开始保存并透传 `tool_call_args`，`search_corpus / web_search / query_library_metadata` 可消费结构化 `query/top_k/max_results`；新增 `app/core/agent_settings.py` 集中 `max_agent_steps / confidence_floor / clarification / disambiguation` 阈值，并让 runtime 读取配置化步数。SSE payload 增加兼容式 `type/name/input/output/ok` 协议字段；新增 `todo_write` 工具，可维护前端可见 TODO 并写入 session working memory。继续接入 `Task` 子任务工具，先复用现有 contract/planner/runtime 执行独立子任务并汇总回答；新增 `fetch_url` 原子工具与 SSRF 安全校验层，仅允许 HTTPS 且拒绝 localhost/private/link-local/reserved 地址。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 158 个测试。
- 2026-04-28 提交首个本地 git baseline：`a67007b chore: baseline pdf rag agent v4`，`.env / data / caches / pyc` 已通过 `.gitignore` 排除。随后继续按 review 推进可观测性与 learnings：每轮 Agent 事件写入 `data/traces/<session>/<turn>.jsonl`，final 只保留 answer preview/chars 以便 eval trace diff；新增 `remember` 工具和 `data/learnings/*.md` 持久化，`_session_conversation_context` 会注入 `persistent_learnings`。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 160 个测试。
- 2026-04-28 按 review 继续拆分检索原子工具：`DualIndexRetriever` 新增 block BM25、`bm25_search / vector_search / hybrid_search / rerank_evidence`；Agent 工具 manifest 与 research registry 接入 `bm25_search / vector_search / hybrid_search / rerank`，让 LLM 可以选择具体检索策略而不是只能调用粗粒度 `search_corpus`。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 162 个测试。
- 2026-04-28 继续按 review §5 补齐本地语料原子工具：`read_pdf_page` 可按 `paper_id + page range` 读取已索引 PDF page/table/caption blocks，`grep_corpus` 可对 paper cards / PDF blocks 做限长正则精确查找；research registry 会把结果并入 evidence/candidate paper state。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 163 个测试。
- 2026-04-28 继续按 review §5 补齐推理原子工具：新增 `summarize` / `verify_claim` 的可测试本地实现，支持对文本、EvidenceBlock、fetch_url payload 和 inline evidence 做摘要/claim 覆盖校验；conversation/research registry 均可执行并记录 `summaries / tool_verifications`。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 168 个测试。
- 2026-04-28 按 review §5 删除旧 solver/retrieval 工具别名入口：`search_papers / search_evidence / solve_claims / verify_grounding` 不再被 canonical 工具映射接纳，内部阶段改用真实工具名 `search_corpus / compose / verify_claim` 上报并保留 `stage` 字段。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 169 个测试。
- 2026-04-28 按 review §6 起步动态工具注册：新增 `propose_tool` schema 与危险标记，conversation/research registry 可把新工具提案写入 `data/tools_proposed/*.json` 待人工审核，当前不执行 `python_code`，为后续 sandbox/approve 流程留口。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 171 个测试。
- 2026-04-28 按 review §7 起步不确定性机制：新增 `Confidence` / `confidence_from_contract` / `should_ask_human`，runtime 与 planner 的澄清触发统一走 `agent_settings.confidence_floor`，保留 `ambiguous_slot` / `low_intent_confidence` 作为置信度来源而不是散落硬判断。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 174 个测试。
- 2026-04-28 继续推进 review §7：新增 `confidence_from_self_consistency` 和 `confidence_from_verification_report`，runtime 在最终 verification 后输出统一 `confidence` state/SSE 事件，runtime summary 的 grounding 增加 verifier confidence，为后续 N=3 采样与 cheap verifier 接入留接口。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 177 个测试。
- 2026-04-28 按 review §8 起步去硬编码 intent：新增旁路 `LLMIntentRouter` 与 `answer_directly / need_corpus_search / need_web / need_clarify` 四个真实 schema router tools，可把 tool-call payload 归一化为 `RouterDecision`，暂不替换旧 `IntentRecognizer`，为逐段迁移 marker 规则留稳定接口。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 181 个测试。
- 2026-04-28 按 review §9 起步检索层工具化：新增 `query_rewrite` 原子工具和本地 deterministic rewrite fallback，支持 `multi_query / hyde / step_back` 模式；research registry 会记录 `query_rewrites / rewritten_queries`，atomic search/grep 可复用首个改写查询。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 185 个测试。
- 2026-04-28 继续推进 review §9：`rerank` schema 扩展为 `query, candidates[], top_k, focus[]`，research registry 会优先重排显式 candidates，没有传入时仍兼容当前 state evidence，向“screen/expand evidence 都变成纯 rerank 工具输入”的方向收口。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 186 个测试。
- 2026-04-28 继续推进 review §9：`search_corpus` 增加可选 `strategy:auto|legacy|bm25|vector|hybrid`，显式传入原子策略时会委托对应 `*_search` 工具路径，默认仍保留旧 paper+evidence 兼容流程，便于逐步迁出粗粒度检索。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 187 个测试。
- 2026-04-28 继续推进 review §9：新增 `retrieval_filter_formula_heavy_non_formula` 配置项，默认保持非公式类实体检索中过滤 formula-heavy snippet 的旧行为，但可关闭该 heuristic，避免检索层硬规则不可控。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 188 个测试。
- 2026-04-28 按 review §11 收口 subprocess 安全：确认 `agent.py` 中 `subprocess` 用于 PDF page 渲染，保留但增加 `ALLOWED_SUBPROCESS_COMMANDS={"pdftoppm"}` 白名单，只允许 bare `pdftoppm` 命令，阻断路径形式或其他命令。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 189 个测试。
- 2026-04-28 按 review §10 补 tool 维度 Prometheus 指标：新增 `tool_calls_total{name,ok}` 与 `tool_latency_seconds{name}`，Agent 工具执行器负责真实 latency/成功失败计数，事件归一化层覆盖旧 observation 路径并避免执行器内双计数。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 193 个测试。
- 2026-04-28 按 review §10/M3 起步 eval trace 回归：新增 `agent_trace_diff` 稳定签名比较与 `scripts/diff_agent_traces.py` CLI，可对 `data/traces/*.jsonl` 做事件序列、工具名、ok/count/status、final execution nodes 的回归 diff，同时忽略 id/took_ms/answer_preview 等易变字段。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 195 个测试。
- 2026-04-28 按 review §11 加固 prompt injection 边界：新增 `prompt_safety.wrap_untrusted_document_text`，将核心 LLM 证据路径中的 PDF/Web snippet 包进 `<document>...</document>` 并 XML escape，同时在 claim 抽取、claim 验证、最终回答和 Web 证据整理 system prompt 中声明 document 内指令不得执行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 197 个测试。
- 2026-04-28 按 review M1 起步拆分 `agent.py` emit/trace 边界：新增 `agent_emit.AgentEventRecorder` 统一事件归一化、缓存和 SSE callback 转发，新增 `write_turn_trace_safe` 收口 trace 写入与异常隔离，`ResearchAssistantAgentV4._run` 不再内联事件 recorder。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 201 个测试。
- 2026-04-28 继续按 review M1 拆分运行上下文：新增 `agent_context.AgentRunContext` 收口单 turn 的 `session_id/session/events/execution_steps/emit`，`ResearchAssistantAgentV4._run` 通过上下文对象传递运行时状态，为后续拆 `agent_loop.py` 降低参数散落。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 202 个测试。
- 2026-04-28 继续按 review M1 拆 `agent_loop.py`：新增 `run_conversation_turn`，把 `_run` 中 conversation 工具执行、clarification/session commit、response payload 组装迁出 `agent.py`，保留现有 runtime 与私有 helper 行为不变。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 204 个测试。
- 2026-04-28 继续按 review M1 拆 `agent_loop.py`：新增 `run_research_turn`，把 `_run` 中 research tool loop、best-effort clarification limit、compose、active research memory、session commit 和 response payload 组装迁出 `agent.py`。`_run` 现在主要负责 contract/plan、conversation/research 分流和 trace 收尾。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 205 个测试。
- 2026-04-28 继续按 review M1 收口 `agent_loop.py` trace 出口：新增 `finish_agent_turn`，统一 final payload trace 写入与 `(payload, events)` 返回，删除 `ResearchAssistantAgentV4._write_turn_trace` 内联出口逻辑，`_run` 三条返回路径共用同一收尾 helper。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 206 个测试。
- 2026-04-28 继续按 review M1 薄化 `_run`：新增 `run_compound_turn_if_needed` 与 `run_standard_turn`，把 compound 兼容入口、contract 抽取、web flag、agent plan、conversation/research 路由全部移入 `agent_loop.py`。`ResearchAssistantAgentV4._run` 现在只负责 session/context 初始化、发出 session 事件、调用 loop 与统一 finish。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 208 个测试。
- 2026-04-28 按 review §6/§11 继续加固动态工具提案：`propose_tool` 新增静态 sandbox 前置检查，要求 `async def run(...)`，只允许安全 import 白名单，并拒绝 `eval/exec/open/__import__/os/subprocess/socket/httpx/urllib/pathlib/shutil` 等执行、文件和网络入口；pending proposal payload 增加 `sandbox_required` 与 `safety` 报告。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 210 个测试。
- 2026-04-28 继续按 review §4/M2 推进 Task 工具化：新增 `agent_task.run_task_subagent` 与 `task_plan_with_allow_list`，把 `Task` 工具内部的 contract/plan/子循环执行/结果组装抽成可复用执行器，registry 只保留输入读取、state 写入和 observation 记录，为后续 compound 迁移到 Task 路径做准备。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 212 个测试。
- 2026-04-28 继续按 review §4.3/M2 推进 compound → `Task` 迁移：`run_task_subagent` 现在可接收预分解 `QueryContract`，避免 compound 子任务重复走旧 intent 抽取；`_execute_compound_research_subtask` 和 conversation 子任务统一委托 Task 子执行器，旧方法保留为兼容壳，compound 路径开始从手写子循环转向工具化子 Agent。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 213 个测试。
- 2026-04-28 继续按 review M1/M2 削薄巨石 Agent：新增 `agent_compound.run_compound_query_if_needed`，把 compound 前置兼容入口与 clarification response 从 `ResearchAssistantAgentV4` 迁出，`agent_loop.py` 直接调用独立模块；`agent.py` 减少约 200 行，compound 行为保持兼容并继续走 Task 子执行器。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 213 个测试。
- 2026-04-28 继续按 review §3.4/M2 删除旧前置短路：移除已无调用点的 `_run_library_citation_ranking_if_needed` 与 `_run_memory_synthesis_if_needed`，citation ranking 和 memory synthesis 继续由 conversation tool registry 中的工具路径承接，减少 `agent.py` 中绕过标准 tool loop 的兼容入口。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 213 个测试。
- 2026-04-28 继续按 review §3.1/§8 去硬编码重复入口：新增 `library_intents.py` 集中库状态、库推荐、引用排序的兼容 classifier，删除 `intent.py` 与 `agent.py` 中重复的 library/citation marker 函数，旧规则只保留一个来源，后续可整体替换为 `LLMIntentRouter`。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 215 个测试。
- 2026-04-28 继续按 review §3.1/§8 收敛 H1-H3：新增 `research_intents.py` 集中 origin / metric / summary 兼容 classifier，删除 `IntentRecognizer` 类内对应 marker 函数，`intent.py` 只调用独立 classifier，为后续把这些规则整体替换为 LLM router 决策留出边界。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 216 个测试。
- 2026-04-28 继续按 review §3.1/§8 收敛 H7-H9：新增 `followup_intents.py` 集中公式解释、语言偏好、公式纠错、论文范围纠错、active paper reference、上下文 metric 与负向纠正 classifier，`agent.py` 删除对应 marker 函数并仅保留运行时状态编排/兼容 wrapper。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 219 个测试。
- 2026-04-28 继续按 review §3.1/§8 收敛硬编码判断：`followup_intents.py` 接管公式查询能否绑定当前论文的上下文/实体名判断，`research_intents.py` 接管最新/引用数等外部搜索触发判断，并让 `IntentRecognizer` 复用同一 helper；`agent.py` 删除对应 marker 块，仅保留运行时参数拼装。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 221 个测试。
- 2026-04-28 继续按 review §3.1/§8 收敛 clarification 选择解析：新增 `clarification_intents.py` 集中“第几个/选哪个/choose/select”等 pending clarification 文字选择 classifier，`agent.py` 删除对应静态 marker 方法，只保留 option 匹配。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 223 个测试。
- 2026-04-28 继续按 review §4.3/M2 薄化 compound 入口：新增 `compound_intents.py` 集中复合查询预判 heuristic，`agent.py` 只负责计算 target 数和 session memory context，再交给纯函数判断是否进入 LLM compound decomposition。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 226 个测试。
- 2026-04-28 继续按 review §3.1/§8 收敛 follow-up relationship heuristic：新增 `followup_relationship_intents.py` 集中严格后续复查 cue、关系词窗口、domain signal、relevance score 和 seed intro signal，`agent.py` 的 follow-up 排序/继承逻辑只保留状态与打分编排。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 229 个测试。
- 2026-04-28 继续按 review §3.1/§8 清理 `agent.py` 剩余 marker 块：`library_intents.py` 接管“全库/所有/全部”范围判断，新增 `figure_intents.py` 集中 Figure 1/benchmark 信号、显式图引用和 benchmark alias 抽取；Agent 保留 mixin 兼容薄 wrapper。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 230 个测试。
- 2026-04-28 继续按 review §3.2 收敛 goal → solver 硬编码：新增 `research_planning.py` 集中 research goals、relation compatibility、paper/evidence limit、solver sequence 和 required claims 映射，`agent.py` 仅保留兼容薄 wrapper，便于后续替换为真正 tool-use TODO/compose 路径。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 233 个测试。
- 2026-04-28 继续按 review M1/M2 削薄 Agent 工具实现边界：新增 `citation_ranking.py` 集中 citation count 解析、title overlap、web evidence 抽取和 citation ranking answer formatting；`agent.py` 保留 web lookup 与 registry 兼容 wrapper，conversation 工具路径行为不变。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 236 个测试。
- 2026-04-28 继续按 review M1/M2 削薄 Web evidence 工具边界：新增 `web_evidence.py` 集中 Web query/topic/domain、evidence merge、Web claim 决策与 claim payload 构造，`agent.py` 保留 `_collect_web_evidence` 与兼容 wrapper。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 239 个测试。
- 2026-04-28 继续按 review M1/M2 削薄本地检索 query shaping 边界：新增 `query_shaping.py` 集中 target 抽取、paper/evidence query 构造、concept evidence 判定、短 acronym 和 Web flag 选择，`agent.py` 保留兼容 wrapper；同时修正 `DPO-v2` 这类混合目标被抽成 `DPO-` 的尾连字符毛刺。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 243 个测试。
- 2026-04-28 继续按 review M1/M2 拆分 evidence presentation 边界：新增 `evidence_presentation.py` 集中 evidence id 收集、Citation payload 构造、topology/formula term 抽取、figure context 排序与 fallback summary、短文本拼接和年份/分块工具，`agent.py` 保留兼容 wrapper 并继续瘦身。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 247 个测试。
- 2026-04-28 继续按 review §2.2/M1 收口 `QueryContract.notes` 隐式协议边界：新增 `contract_context.py` 集中 note 读取、answer slot/topic state 解析、conversation relation 是否更新 research context、observed/canonical tool name 归一化和 intent kind 推断，`agent.py` 保留兼容 wrapper，为后续替换 magic notes bus 做准备。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 250 个测试。
- 2026-04-28 继续按 review M1/M2 收口 contract normalization 边界：新增 `contract_normalization.py` 集中 target suffix 清洗、结构化 Figure/Table/Formula 目标过滤、target 去重/规范化与 modality alias/default 规则，`agent.py` 继续保留兼容 wrapper，减少硬编码散落。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 253 个测试。
- 2026-04-28 继续按 review §8 推进 intent 去硬编码：新增 `memory_intents.py` 集中上一轮/列表/序号引用、短追问、memory comparison 与 PDF-Agent topology 兼容 classifier，`IntentRecognizer` 保留薄 wrapper，继续削薄 `intent.py`。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 256 个测试。
- 2026-04-28 继续按 review M1/M2 削薄 tool registry：新增 `tool_registry_helpers.py` 集中 TODO 归一化/session 保存、Task/fetch/summarize compose 文本格式化、state evidence 汇总、summary source 拼接、focus 和 int 参数约束，`agent_tool_registries.py` 只保留 registry 执行编排。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 260 个测试。
- 2026-04-28 继续按 review M1/M2 收口 tool registry 小协议：`tool_registry_helpers.py` 继续接管 `tool_inputs` 读取、conversation/research intent summary payload 构造，消除 conversation/research registry builder 的重复样板。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 261 个测试。
- 2026-04-28 继续按 review §5/M1 削薄 fetch_url 工具执行边界：`tool_registry_helpers.py` 接管 `FetchUrlResult` payload 构造与 Web EvidenceBlock 生成，conversation/research registry 共用同一转换逻辑。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 262 个测试。
- 2026-04-28 继续按 review M1/M3 解除 planning ↔ solver 反向耦合：新增 `solver_goal_helpers.py` 集中 metric goal、query fallback goals 与 solver claim goals 推断，`research_planning.py` 不再依赖 `SolverPipelineMixin`，`solver_pipeline.py` 保留兼容 wrapper。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 268 个测试。
- 2026-04-28 继续按 review §3.2/§6 削薄 solver 黑盒：新增 `formula_text_helpers.py` 集中公式 payload 候选、变量符号规范化、LaTeX 化、公式窗口选择、梯度意图判断与 formula block 打分，`solver_pipeline.py` 保留兼容 wrapper 并降至 1450 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 272 个测试。
- 2026-04-28 继续按 review §3.2/§6 削薄 metric/table solver 边界：新增 `metric_text_helpers.py` 集中 metric 行抽取、token 权重打分与 block 排序基础分，`solver_pipeline.py` 只保留 settings 注入和 primary-paper 匹配 wrapper，并降至 1425 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 275 个测试。
- 2026-04-28 继续按 review §5/M1 收口置信度小协议：`confidence.py` 新增 `coerce_confidence_value`，统一 high/medium/low 与数字字符串解析，`solver_pipeline.py` 和 `entity_definition.py` 不再各自手写 confidence coercion，同时保留各调用点原默认分值。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 276 个测试。
- 2026-04-28 继续按 review §3.2/§6 削薄 topology solver 边界：新增 `topology_recommendation_helpers.py` 集中 topology 推荐 fallback 与不可用推荐文本判定，`solver_pipeline.py` 只保留 LLM 调用与兼容 wrapper，并降至 1404 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 278 个测试。
- 2026-04-28 继续按 review §3.2/§6 削薄 origin solver/verifier 边界：新增 `origin_selection_helpers.py` 集中 origin 目标别名、paper 文本拼接、展示实体名、intro/definition scoring 与 intro support 判定，`solver_pipeline.py` 保留兼容 wrapper 并降至 1266 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 283 个测试。
- 2026-04-28 继续按 review §6/M3 收口通用 schema claim solver 边界：新增 `schema_claim_helpers.py` 集中 LLM schema payload 校验、evidence/paper id 过滤、Claim 构造与 confidence coercion，`solver_pipeline.py` 只保留 schema prompt/LLM 调用并降至 1221 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 285 个测试。
- 2026-04-28 继续按 review §3.2/§6 收口 formula solver LLM payload 边界：`formula_text_helpers.py` 接管 LLM formula response 的 evidence id 白名单过滤、变量规范化、format 推断和 terms 构造，`solver_pipeline.py` 仅保留公式抽取 prompt/LLM 调用并降至 1191 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 286 个测试。
- 2026-04-28 继续按 review §6/M3 收口视觉 solver payload 边界：新增 `visual_claim_helpers.py` 集中 table/figure VLM JSON payload 到 Claim 的转换、metric lines fallback、figure signal gate 与 confidence coercion，`solver_pipeline.py` 只保留渲染/多模态调用编排并降至 1180 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 289 个测试。
- 2026-04-28 继续按 review §3.2/§6 削薄 followup solver 边界：新增 `followup_claim_helpers.py` 集中 followup_research Claim 的 seed/followup payload、evidence ids、paper ids、mode 与平均 confidence 组装，`solver_pipeline.py` 保留 seed/candidate ranking 编排并降至 1133 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 291 个测试。
- 2026-04-28 继续按 review §3.2/§6 削薄 paper solver Claim 构造边界：新增 `paper_claim_helpers.py` 集中 paper_summary、paper_recommendation 与 default text Claim 构造，`solver_pipeline.py` 保留 paper 选择、summary 读取与 metric evidence 编排并降至 1101 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 294 个测试。
- 2026-04-28 继续按 review §3.2/§6 削薄 topology solver Claim 构造边界：`topology_recommendation_helpers.py` 接管 topology_discovery/topology_recommendation Claim 构造、evidence/paper ids 去重与 top evidence 选择，`solver_pipeline.py` 保留 topology term 抽取与 LLM recommendation 编排并降至 1068 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 296 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄 formula solver Claim/payload 边界：`formula_text_helpers.py` 接管 fallback formula payload 生成、formula Claim 构造、terms/evidence/confidence 组装，`solver_pipeline.py` 保留公式证据筛选与 LLM 调用编排并降至 1025 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 298 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄 metric solver Claim 构造边界：`metric_text_helpers.py` 接管 metric_context 与 text_table metric Claim 构造、metric lines/paper titles/evidence ids 组装，`solver_pipeline.py` 保留 metric evidence 排序与 VLM/text fallback 编排并降至 1020 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 300 个测试。
- 2026-04-29 继续按 review §3.2/§6 清理 solver 兼容壳：删除 `solver_pipeline.py` 中已无调用点的公式 payload/变量/LaTeX/window helper wrapper，仅保留外部旧测试覆盖的公式归一化入口，`solver_pipeline.py` 首次降至 985 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 300 个测试。
- 2026-04-29 继续按 review M1/M2 削薄 tool registry 小协议：`tool_registry_helpers.py` 新增 `verify_claim_tool_payload`，统一 conversation/research registry 中 verify_claim 的 evidence 解析、min_overlap 约束、payload/summary 构造；`agent_tool_registries.py` 降至 1012 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 301 个测试。
- 2026-04-29 继续按 review M1/M2 削薄 tool registry 小协议：`tool_registry_helpers.py` 新增 `summarize_tool_payload`，统一 conversation/research registry 中 summarize 的 focus/target_words、text/evidence/source fallback 与 payload 构造，`agent_tool_registries.py` 降至 995 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 302 个测试。
- 2026-04-29 继续按 review M1/M2 削薄 tool registry 小协议：`tool_registry_helpers.py` 新增 `todo_write_tool_payload` 与 `remember_tool_payload`，统一 conversation/research registry 中 TODO 归一化、session 写入、learnings 持久化和 observation payload 构造，`agent_tool_registries.py` 降至 986 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 303 个测试。
- 2026-04-29 继续按 review M1/M3 收口动态工具提案边界：`tool_registry_helpers.py` 新增 `propose_tool_payload`，把 `propose_tool` 的 data_dir 解析、schema/code 校验、pending/rejected payload 组装移出 registry，并覆盖 pending/rejected 两条测试路径，`agent_tool_registries.py` 降至 961 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 304 个测试。
- 2026-04-29 继续按 review M1/M2 收口 registry 输入小协议：`tool_registry_helpers.py` 新增 `planned_tool_input_from_state`，统一 `tool_inputs[name]` 优先、`current_tool_input` fallback 的读取逻辑，conversation/research registry 不再各自手写 fallback 样板，`agent_tool_registries.py` 降至 959 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 304 个测试。
- 2026-04-29 继续按 review M1/M2 削薄 read_memory 工具边界：`tool_registry_helpers.py` 新增 `read_memory_tool_payload`，统一 conversation/research registry 中 active research context、turn_count、working memory 标记和 title 截断策略，`agent_tool_registries.py` 降至 951 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 305 个测试。
- 2026-04-29 继续按 review M1/M2 收口 fetch_url 工具边界：`tool_registry_helpers.py` 新增 `fetch_url_tool_request` 与 `fetch_url_tool_payload`，统一 conversation/research registry 中 url/max_chars 解析、fetched_urls state payload 与 observation payload 构造，同时保持 HTTP 调用和 tool_call 事件顺序不变，`agent_tool_registries.py` 降至 950 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 305 个测试。
- 2026-04-29 继续按 review M1/M2 削薄 research retrieval registry：`tool_registry_helpers.py` 新增 `read_pdf_page_tool_request`、`grep_corpus_tool_request` 与 `query_rewrite_tool_request`，统一页码/字符上限、regex fallback、paper_ids 清洗、query rewrite targets/max_queries 约束，`agent_tool_registries.py` 降至 907 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 306 个测试。
- 2026-04-29 继续按 review M1/M2 削薄 research retrieval registry：`tool_registry_helpers.py` 新增 `atomic_search_tool_request`、`atomic_search_observation_payload`、`rerank_tool_request` 与 `rerank_observation_payload`，统一原子检索和 rerank 的 query/scope/paper_ids/focus/top_k/candidates 解析及 observation payload 构造，`agent_tool_registries.py` 降至 871 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 307 个测试。
- 2026-04-29 继续按 review M1/M2 收口 research evidence result 小协议：`tool_registry_helpers.py` 新增 `store_research_evidence_result`、`evidence_event_payload` 与 `evidence_result_observation_payload`，统一 evidence 合并、screened/candidate papers 补全、evidence event 和 observation payload 构造，`agent_tool_registries.py` 降至 863 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 308 个测试。
- 2026-04-29 继续按 review M1/M2 收口 research registry 小协议：原子检索改用统一 `store_research_evidence_result` 路径，`tool_registry_helpers.py` 新增 `query_rewrite_tool_payload`、`search_corpus_strategy` 与 `search_corpus_observation_payload`，统一 query_rewrite state 写入和 search_corpus observation 构造，`agent_tool_registries.py` 降至 846 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 310 个测试。
- 2026-04-29 继续按 review M1/M2 收口 Task 工具边界：`tool_registry_helpers.py` 新增 `task_tool_request` 与 `task_result_observation_payload`，统一 conversation registry 中 Task prompt/tools_allowed/max_steps 解析和子任务 observation payload 构造，`agent_tool_registries.py` 降至 838 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 311 个测试。
- 2026-04-29 继续按 review M1/M2 收口 registry state 小协议：`tool_registry_helpers.py` 新增 `store_fetch_url_evidence_result` 与 `store_claim_check_payload`，统一 fetch_url web evidence 写入/事件 payload 和 verify_claim 的 claim_checks/tool_verifications 双写逻辑，`agent_tool_registries.py` 降至 836 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 312 个测试。
- 2026-04-29 继续按 review M1/M2 削薄 conversation registry：`tool_registry_helpers.py` 新增 `library_metadata_tool_request` 与 `library_metadata_observation_payload`，统一 query_library_metadata 的 query fallback、tool_call arguments 和 observation payload 构造，并移除 registry 内最后一个本地 `tool_input` 闭包，`agent_tool_registries.py` 降至 828 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 313 个测试。
- 2026-04-29 继续按 review M1/M2 削薄 conversation compose 边界：`tool_registry_helpers.py` 新增 `conversation_artifact_answer_from_state`，统一 Task/fetch/summarize 既有工具产物到 answer/citations 的优先级与格式化逻辑，`agent_tool_registries.py` 降至 814 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 314 个测试。
- 2026-04-29 继续按 review M1/M2 收口 clarify/compose observation 小协议：`tool_registry_helpers.py` 新增 `conversation_clarification_report`、`compose_done_payload`、`reflect_previous_answer_payload`、`ensure_research_clarification_report` 与 `research_compose_observation_payload`，统一 conversation/research 的 ask_human、compose 和 reflection payload，并删除 conversation `read_memory` 纯转发 wrapper，`agent_tool_registries.py` 降至 798 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 315 个测试。
- 2026-04-29 继续按 review M1/M2 收口 citation ranking 小协议：`tool_registry_helpers.py` 新增 `store_citation_candidates_payload`、`store_citation_lookup_payload` 与 `citation_ranking_result_payload`，统一 conversation registry 中引用数候选恢复、web lookup 结果记录、citation doc ids 和 verification_report 构造，`agent_tool_registries.py` 降至 792 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 316 个测试。
- 2026-04-29 继续按 review M1/M2 收口共享 state/answer 小协议：`tool_registry_helpers.py` 新增 `store_fetched_url_payload`、`store_summary_payload`、`store_tool_proposal_payload` 与 `store_conversation_answer_result`，统一 fetched_urls/summaries/tool_proposals append，以及 conversation answer 的记忆写入、answer 设置和 chars payload，`agent_tool_registries.py` 降至 791 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 317 个测试。
- 2026-04-29 继续按 review M1/M2 收口 tool observation envelope：`tool_registry_helpers.py` 新增 `record_tool_observation`，conversation/research registry 共用同一个 observation 记录入口，`agent_tool_registries.py` 降至 736 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 318 个测试。
- 2026-04-29 继续按 review M1/M2 收口 runtime/tool-input 小协议：`tool_registry_helpers.py` 新增 `tool_inputs_by_name` 与 `tool_loop_ready_observation`，`AgentRuntime` 复用统一 `tool_input_from_state`，移除 runtime 内部 tool_call_args 索引和 ready observation 拼装私有方法，`agent_runtime.py` 降至 335 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 318 个测试。
- 2026-04-29 继续按 review M1/M2 收口 runtime fallback action 策略：新增 `agent_runtime_helpers.py`，把 max steps 解析、contract 澄清判定、conversation/research fallback next-action 规则移成可测试纯函数，`agent_runtime.py` 降至 274 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 322 个测试。
- 2026-04-29 继续按 review M1/M2 收口 planner 工具协议边界：新增 `agent_planner_helpers.py`，把 planner intent/context payload、state summary、fallback plan 与 plan payload 归一化移成可测试纯函数，`agent_planner.py` 降至 207 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 326 个测试。
- 2026-04-29 继续按 review M1/M2 收口 planner prompt/tool manifest 协议：`agent_planner_helpers.py` 集中 JSON/tool-call/next-action system prompt 和 context suffix，`agent_tools.py` 新增 `agent_tool_manifest_for_names` 统一 allowed tool manifest 过滤，`agent_planner.py` 降至 193 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 327 个测试。
- 2026-04-29 继续按 review M1/M2 收口 runtime state 初始化协议：`agent_runtime_helpers.py` 新增 conversation/research runtime state 构造、agent loop summary 与 ready tool 选择 helper，`AgentRuntime` 不再内联 state 字典和 loop summary 拼接，`agent_runtime.py` 降至 262 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 328 个测试。
- 2026-04-29 继续按 review M1/M2 收口 research verification 收尾协议：`agent_runtime_helpers.py` 新增 `finalize_research_verification` 与 `verification_execution_step`，统一 reflection 后 verification 默认补全、confidence payload 写入和 verify_claim execution step 记录，`agent_runtime.py` 降至 251 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 329 个测试。
- 2026-04-29 继续按 review M1/M2 收口 runtime action 序列协议：`agent_runtime_helpers.py` 新增 conversation/research action 解析和 `dequeue_action`，`AgentRuntime` 不再直接读取 planner actions 或维护队列弹出逻辑，`agent_runtime.py` 降至 242 行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 330 个测试。
- 2026-04-29 继续按 review M1/M2 收口 runtime planner-next-action 协议：`agent_runtime_helpers.py` 新增 `planner_next_action`，把 planner fallback 调用从 `AgentRuntime` 私有方法移成可测试 helper，`agent_runtime.py` 降至 223 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 331 个测试。
- 2026-04-29 继续按 review §4/M1 收口 runtime tool-use loop 边界：`agent_runtime_helpers.py` 新增 `execute_tool_loop`，统一 planned action、planner fallback、fallback_next、tool input 注入和 stop condition 执行循环，`AgentRuntime` 只保留 conversation/research 两个入口，`agent_runtime.py` 降至 172 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 332 个测试。
- 2026-04-29 继续按 review §4/M1 收口 runtime 事件与 verification 收尾协议：`agent_runtime_helpers.py` 新增 `record_tool_loop_ready`、`agent_loop_execution_step` 与 `finalize_research_runtime`，统一 ready observation、agent_loop execution step、reflection 后 verification/confidence 事件写入，`agent_runtime.py` 降至 165 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 334 个测试。
- 2026-04-29 继续按 review M1/M2 收口 planner LLM 调用协议：`agent_planner_helpers.py` 新增 JSON planner、tool-call planner、next-action planner 的 prompt/messages/human payload 构造与首个未执行 action 选择 helper，`AgentPlanner` 不再内联 JSON 拼装，`agent_planner.py` 降至 173 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 336 个测试。
- 2026-04-29 继续按 review §4.2/M1 推进 SSE/trace 事件协议统一：`agent_events.py` 为 `plan`、`verification`、`confidence` 增加兼容式标准字段，保留旧 payload 同时补充 plan items、verification payload/status 与 confidence value/basis，方便前端和 trace diff 逐步消费稳定协议。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 337 个测试。
- 2026-04-29 继续按 review §8 收敛 intent 硬编码：`research_intents.py` 新增 `research_answer_slots`，集中后续研究、公式、图表、实验结果、metric、论文推荐、topology、training component 和 definition slot 判断，`IntentRecognizer._research_slots` 降为薄调用，`intent.py` 降至 1002 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 338 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄 solver schema gate：`schema_claim_helpers.py` 新增 `should_use_schema_claim_solver` 与 blocked high-precision goal 集合，`solver_pipeline.py` 不再内联通用 schema solver 的禁用目标判断，并降至 972 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 339 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄通用 schema claim solver：`schema_claim_helpers.py` 新增 `schema_claim_system_prompt` 与 `schema_claim_human_prompt`，统一通用 Claim 抽取器 prompt、papers/evidence payload 和 `<document>` 不可信证据包裹，`solver_pipeline.py` 不再内联 schema claim prompt/payload 并降至 931 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 340 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄公式 solver：`formula_text_helpers.py` 新增 `formula_extractor_system_prompt` 与 `formula_extractor_human_prompt`，统一公式抽取器 prompt、contract/evidence payload 与 evidence 截断策略，`solver_pipeline.py` 不再内联公式 LLM prompt/payload 并降至 896 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 341 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄视觉 solver：`visual_claim_helpers.py` 新增 table/figure VLM system prompt 与 multimodal human content 构造 helper，统一表格/图像上下文文本、页面图片去重和 image_url 组装，`solver_pipeline.py` 不再内联 table/figure VLM content builder 并降至 856 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 343 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄 topology solver：`topology_recommendation_helpers.py` 新增 topology recommendation system/human prompt 与 payload 归一化 helper，统一证据截断、不可用回答过滤和 fallback recommendation，`solver_pipeline.py` 不再内联 topology recommendation LLM 协议并降至 840 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 344 个测试。
- 2026-04-29 继续按 review §3.2/§6 收口 deterministic solver 编排协议：`solver_goal_helpers.py` 新增 `append_unique_claims`，统一 deterministic fallback 中按 `(claim_type, entity, value)` 去重追加 Claim 的逻辑，`solver_pipeline.py` 去掉局部 `extend_once` 闭包并降至 831 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 345 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄 origin solver Claim 构造边界：`origin_selection_helpers.py` 新增 `origin_lookup_claim`，集中 origin Claim 的 display entity、structured data、paper id 与 doc fallback 组装，`solver_pipeline.py` 只保留 origin paper 选择和 evidence id 查询并降至 816 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 346 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄视觉 solver fallback 边界：`visual_claim_helpers.py` 新增 `figure_conclusion_text_claim`，集中 figure text-summary/caption fallback Claim 的 mode、evidence ids、paper id 与 confidence 组装，`solver_pipeline.py` 复用统一 figure context/entity 并降至 815 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 347 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄 entity definition solver 边界：新增 `entity_definition_helpers.py`，集中 entity_definition Claim 构造和 evidence/doc fallback id 选择，`solver_pipeline.py` 只保留 supporting paper/evidence 选择、entity type 推断与 supporting lines 编排并降至 801 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 350 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄公式 solver 证据选择边界：`formula_text_helpers.py` 新增 `formula_target_terms`、`formula_matched_targets` 与 `select_formula_blocks`，集中公式 target 匹配、强公式块排序和 keyword fallback 逻辑，`solver_pipeline.py` 不再内联公式块筛选并降至 795 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 352 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄 metric/table solver 证据选择边界：`metric_text_helpers.py` 新增 `ranked_metric_context_evidence`、`ranked_table_metric_blocks` 与 `metric_paper_selection`，集中 metric evidence 过滤、table/page_text 阈值、排序和主 paper 选择，`solver_pipeline.py` 移除本地 metric scoring wrappers 并降至 766 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 354 个测试。
- 2026-04-29 继续按 review §3.2/§6 清理 solver/origin 兼容壳：`claim_verifier.py` 与 `entity_definition.py` 直接复用 `origin_selection_helpers.py`，`solver_pipeline.py` 删除 goal/origin 私有转发 wrapper，减少 mixin 之间通过私有方法互相耦合，并降至 737 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 354 个测试。
- 2026-04-29 继续按 review §3.2/§6 削薄 paper summary/default solver 边界：`paper_claim_helpers.py` 新增 `paper_summary_claims` 与 `default_text_claims`，集中 summary/default text Claim 批量组装、空结果跳过和 fallback entity 逻辑，`solver_pipeline.py` 只保留 paper focus 过滤并降至 718 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.1/H8 收敛纠错意图入口：`agent.py` 删除 `_is_negative_correction_query` 私有转发 wrapper，`AgentPlanner`、`AgentRuntime` 和 agent 内部调用统一直接使用 `followup_intents.is_negative_correction_query`，减少 agent 巨石内的重复意图入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.1/H5 收敛 citation ranking 边界：`agent.py` 删除 citation count 解析、标题 overlap 与 citation ranking answer 的私有转发 wrapper，`agent_tool_registries.py` 直接复用 `citation_ranking.format_citation_ranking_answer`，让 citation ranking 协议集中在独立 helper 中，`agent.py` 降至 5721 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.2/M1 清理 research planning 兼容壳：`agent.py` 删除已无调用点的 `_paper_limit_for_goals`、`_evidence_limit_for_goals`、`_solver_sequence_for_goals`、`_required_claims_for_goals`、`_normalize_research_goal` 与 `_goals_from_relation_compatibility`，让 plan 词表逻辑继续集中在 `research_planning.py`，`agent.py` 降至 5693 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §5/M1 收敛 web evidence 协议入口：`agent.py` 删除 web query/topic/include-domain、evidence merge 与 should-add-web-claim 私有转发 wrapper，agent 和 registry helper 直接复用 `web_evidence.py` 的纯函数，保留已被外部测试覆盖的 `_collect_web_evidence`/`_build_web_research_claim` 兼容入口，`agent.py` 降至 5673 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.2/§6 收敛 topology evidence presentation 入口：`answer_composer.py`、`claim_verifier.py` 与 `solver_pipeline.py` 直接复用 `evidence_presentation.extract_topology_terms`，删除 `agent.py` 中 `_extract_topology_terms` 私有转发 wrapper，减少 mixin 对 Agent 私有壳的依赖，`agent.py` 降至 5668 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.2/M1 清理 evidence presentation 兼容壳：删除 `agent.py` 中已无调用点的 `_top_evidence_ids` 与 `_join_unique_text` 私有转发 wrapper，真正实现继续保留在 `evidence_presentation.py` 并由专门测试覆盖，`agent.py` 降至 5658 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.2/§6 收敛公式 evidence presentation 入口：`solver_pipeline.py` 直接把 `evidence_presentation.formula_terms` 传入公式 payload/Claim helper，删除 `agent.py` 中 `_formula_terms` 私有转发 wrapper，`agent.py` 降至 5653 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.2/§6 收敛 evidence id 选择入口：`solver_pipeline.py` 直接复用 `evidence_presentation.evidence_ids_for_paper` 生成 origin/topology/summary/metric/formula 的 fallback evidence ids，删除 `agent.py` 中 `_evidence_ids_for_paper` 私有转发 wrapper，`agent.py` 降至 5648 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.2/M1 收敛 claim evidence id 入口：`answer_composer.py` 直接使用 `evidence_presentation.claim_evidence_ids` 生成 citation doc ids，删除 `agent.py` 中 `_claim_evidence_ids` 私有转发 wrapper，citation 构造兼容入口保持不变，`agent.py` 降至 5643 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §3.2/§6 收敛 figure solver presentation 入口：`solver_pipeline.py` 直接复用 `build_figure_contexts`、`figure_fallback_summary`、`extract_figure_benchmarks` 与 `figure_signal_score`，删除 `agent.py` 中无直接契约的 `_figure_fallback_summary`/`_extract_figure_benchmarks` 转发壳，保留旧测试覆盖的 figure context/score 兼容入口，`agent.py` 降至 5635 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review M1/M2 清理年份排序兼容壳：`entity_definition.py`、`solver_pipeline.py` 与 `agent.py` 内部排序直接复用 `evidence_presentation.safe_year`，删除 `agent.py` 中 `_safe_year` 私有转发 wrapper，`agent.py` 降至 5631 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review M1/M2 清理流式分块兼容壳：`agent.py` 的 SSE fallback/answer delta 与 `agent_compound.py` 的 compound publish 直接复用 `evidence_presentation.chunk_text`，删除 `agent.py` 中 `_chunk_text` 私有转发 wrapper，`agent.py` 降至 5627 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 356 个测试。
- 2026-04-29 继续按 review §2.2/M1 收口 clarification notes 协议：`clarification_intents.py` 接管 clarification option schema version、public payload 白名单和 `ambiguity_option=` notes 解析，`agent.py` 删除对应私有 helper，只保留 option 归一化与会话状态编排，`agent.py` 降至 5583 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 358 个测试。
- 2026-04-29 继续按 review §5/M1 收敛工具名协议来源：删除 `agent.py` 中手写的 `CANONICAL_TOOL_NAMES` 集合，runtime summary、canonical tool 归一化和旧事件兼容全部改为读取 `agent_tools.all_agent_tool_names()`，让 canonical 工具名与 manifest/schema 注册表同源，`agent.py` 降至 5560 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 358 个测试。
- 2026-04-29 继续按 review §5/M1 下沉工具 alias 兼容协议：`contract_context.py` 接管 `LEGACY_TOOL_NAME_ALIASES` 与 `canonical_agent_tool`，`agent.py` 不再内联旧工具名映射表，只调用统一 helper 兼容旧 execution/event 名称，`agent.py` 降至 5546 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 358 个测试。
- 2026-04-29 继续按 review §2.2/M1 清理 contract-context 兼容壳：`agent.py` 内部 runtime summary、active-context override、disambiguation 和 LLM payload 直接复用 `contract_context.py` 的 note/topic/answer-slot helpers，删除无外部契约的私有转发 wrapper，仅保留 `_canonical_tools` 旧测试入口，`agent.py` 降至 5514 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 358 个测试。
- 2026-04-29 继续按 review §4/M1 收敛 loop/context 边界：`agent_loop.py` 直接复用 `contract_context.conversation_relation_updates_research_context` 判断 conversation 结果是否更新 active research，删除 `agent.py` 中同名私有转发 wrapper，`agent.py` 降至 5509 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 358 个测试。
- 2026-04-29 继续按 review §5/M1 清理工具 manifest 转发壳：compound decomposition 的 LLM payload 直接读取 `agent_tools.agent_tool_manifest()`，删除 `agent.py` 中 `_agent_tool_manifest` 私有 wrapper，`agent.py` 降至 5505 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 358 个测试。
- 2026-04-29 继续按 review §3.2/M1 清理 research planning 兼容壳：`agent.py` 与 `followup_routing.py` 直接复用 `research_planning.research_plan_goals`，删除 `agent.py` 中 `_research_plan_goals` 私有 wrapper，让 goal 词表继续集中在 planning helper，`agent.py` 降至 5501 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 358 个测试。
- 2026-04-29 继续按 review M1/M2 清理 query shaping 兼容壳：`agent.py` 内部检索 query、target 抽取、concept evidence 判断和短 acronym 判断直接复用 `query_shaping.py`，`claim_verifier.py` 也直接使用 `is_short_acronym`，删除 `agent.py` 中对应私有 wrapper；保留外部测试覆盖的 web-search/web-claim 兼容入口，`agent.py` 降至 5481 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 358 个测试。
- 2026-04-29 继续按 review §3.2/§6 收敛 origin 选择 helper：`origin_selection_helpers.py` 新增 `pick_origin_paper`，`solver_pipeline.py` 的 origin fallback 直接复用该 helper，删除 `agent.py` 中 `_pick_origin_paper` 私有实现并补单测，`agent.py` 降至 5474 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 359 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 lookup text 归一化入口：`agent.py`、`claim_verifier.py`、`entity_definition.py` 与 `followup_routing.py` 直接复用 `contract_normalization.normalize_lookup_text`，删除 `agent.py` 中 `_normalize_lookup_text` 私有重复实现，减少跨 mixin 的 Agent 私有壳依赖，`agent.py` 降至 5471 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 359 个测试。
- 2026-04-29 继续按 review M1/M2 收敛 target matching 入口：`query_shaping.py` 新增 `matches_target`，`agent.py`、`claim_verifier.py`、`entity_definition.py` 与 `solver_pipeline.py` 直接复用统一 target 边界/公式 loss alias 匹配规则，删除 `agent.py` 中 `_matches_target` 私有实现并补 query shaping 单测，`agent.py` 降至 5451 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 360 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 clarification option 小协议：`clarification_intents.py` 接管 string-list coercion、description fallback 和 stable option id 生成，`agent.py` 删除对应私有 helper，只保留 option 归一化编排，`agent.py` 降至 5412 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 362 个测试。
- 2026-04-29 继续按 review M1/M2 清理标题 key 兼容壳：`agent.py` 与 `answer_composer.py` 直接复用 `contract_normalization.normalize_lookup_text` 作为当前标题去重/排除 key，删除 `agent.py` 中重复定义且前者已被覆盖的 `_normalize_title_key` 私有 helper，`agent.py` 降至 5404 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 363 个测试。
- 2026-04-29 继续按 review §5/M1 清理 tool-input 兼容壳：`agent.py` 的 search/web 工具执行路径直接复用 `tool_registry_helpers.tool_input_from_state` 与 `coerce_int`，删除 `_state_tool_input` 和 `_tool_int_arg` 私有重复实现，工具参数默认值与上下限保持不变，`agent.py` 降至 5397 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 363 个测试。
- 2026-04-29 继续按 review §2.2/M1 清理 contract-normalization 兼容壳：`claim_verifier.py` 与 `followup_routing.py` 直接复用 `contract_normalization` 的 structural-target 与 modality helpers，`agent.py` 删除 `_clean_contract_target_text`、`_is_structural_target_reference` 和 `_normalize_modalities` 私有转发入口，`agent.py` 降至 5383 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 363 个测试。
- 2026-04-29 继续按 review §5/M1 清理 canonical tool 转发壳：`agent.py` 的 tool_call/observation 事件直接调用 `contract_context.canonical_agent_tool`，删除 `_canonical_agent_tool` 私有 wrapper，工具名 alias 兼容继续由 `LEGACY_TOOL_NAME_ALIASES` 和 manifest canonical names 统一驱动。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 363 个测试。
- 2026-04-29 继续按 review §4.2/M1 收敛 agent step message 协议：新增 `agent_step_messages.py`，集中 tool-use step 的前端提示文案和 target/relation 插值，`agent.py` 删除 `_agent_step_message` 大字典，仅在 emit 时调用纯 helper，`agent.py` 降至 5345 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 365 个测试。
- 2026-04-29 继续按 review §4.2/M1 收敛 runtime summary 协议：新增 `agent_runtime_summary.py`，集中 intent/tool_loop/grounding/contract_context payload 构造和 canonical/legacy tool 归一化，`agent.py` 保留 `_runtime_summary` 兼容入口但只负责注入 active research context，`agent.py` 降至 5271 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 367 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 memory follow-up 小协议：新增 `memory_artifact_helpers.py`，集中“上一轮列表第 N 条”引用解析、最近列表 artifact 选择和结构化行格式化，`agent.py` 的 memory follow-up 直接调用纯 helper，删除对应私有解析/格式化实现，`agent.py` 降至 5151 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 371 个测试。
- 2026-04-29 继续按 review §4/M1 收敛会话上下文渲染边界：新增 `session_context_helpers.py`，集中 LLM-facing conversation context、history messages 和 text truncation 规则，`agent.py` 保留兼容入口但只注入 persistent learnings，`agent.py` 降至 5088 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 375 个测试。
- 2026-04-29 继续按 review §4.3/M2 收敛 compound 子任务展示协议：新增 `compound_task_helpers.py`，集中 compound task label、section heading、markdown heading demotion、research progress 文案和 Task payload 结果恢复，`agent_compound.py` 直接复用 helper，`agent.py` 删除对应私有实现并降至 5019 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 378 个测试。
- 2026-04-29 继续按 review §3.2/M1 清理 citation presentation 兼容壳：`evidence_presentation.py` 新增 `dedupe_citations`，compound response、citation ranking registry 和 memory binding citation 恢复统一复用该 helper，删除 `agent.py` 中 `_dedupe_citations` 私有实现并降至 5008 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 378 个测试。
- 2026-04-29 继续按 review M1/M2 清理 Agent 私有转发壳：`agent.py` 内部直接复用 `session_context_helpers.truncate_context_text`、`followup_intents.is_language_preference_followup` 和 `memory_intents.is_memory_comparison_query`，删除剩余 truncation/language/comparison 私有 wrapper，`agent.py` 首次降至 4996 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 378 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 library conversation contract 协议：`library_intents.py` 接管 `library_status_contract` 与 `library_recommendation_contract`，`agent.py` 删除本地纯构造函数并在 conversation contract 归一化时直接复用统一来源，`agent.py` 降至 4968 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 380 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 memory/tool-result artifact 协议：`memory_artifact_helpers.py` 接管 `conversation_tool_result_artifact`，`agent_tool_registries.py` 直接用 helper 压缩 `query_library_metadata` 表格结果，`agent.py` 删除对应私有实现并降至 4938 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 382 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 conversation memory contract 修正规则：新增 `conversation_memory_contract.py`，集中 target binding 恢复、fresh formula 防误绑定、origin/year 查询跳过和 selected paper notes 注入规则，`agent.py` 删除对应私有实现并降至 4898 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 386 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 conversation tool memory 写入协议：`memory_artifact_helpers.py` 接管 `remember_conversation_tool_result`，`tool_registry_helpers.store_conversation_answer_result` 不再回调 Agent 私有方法，工具结果、last_displayed_list 与 library metadata artifact 统一由 helper 写入，`agent.py` 降至 4863 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 387 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 memory synthesis binding 协议：`conversation_memory_contract.py` 接管 `active_memory_bindings` 与 `memory_binding_doc_ids`，`agent_tool_registries.py` 直接用 helper 为 memory synthesis 恢复引用，删除 Agent 中 memory binding/citation 私有壳，`agent.py` 降至 4839 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 389 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 follow-up relationship memory 协议：新增 `followup_relationship_memory.py`，集中 followup_research Claim 的 seed/candidate relationship working memory 构造，`agent.py` 只在 research outcome 写入时调用 helper 并降至 4791 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 392 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 research outcome memory 写入协议：新增 `research_memory.py`，集中 target binding、last_successful_research 和 follow-up relationship working memory 写入规则，`agent.py` 只保留 candidate lookup 注入并降至 4742 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 395 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 compound outcome memory 写入协议：`research_memory.py` 新增 `remember_compound_outcome`，集中 compound 子任务 target binding 恢复、subtask preview 和 last_compound_query 写入，`agent.py` 只保留 candidate lookup 注入并降至 4711 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 396 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 contextual contract 小协议：新增 `contextual_contract_helpers.py`，集中 active paper reference notes 与 contextual metric promotion 规则，paper-scope/active-paper follow-up 复用同一 helper，`agent.py` 降至 4675 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 399 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛公式上下文 contract 构造协议：`contextual_contract_helpers.py` 接管 formula answer correction、formula location follow-up 与 formula contextual paper binding 的 QueryContract 构造，Agent 只保留 paper/target 判定编排，`agent.py` 降至 4604 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 402 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛公式上下文判定纯规则：`contextual_contract_helpers.py` 接管 paper hint names、entity key 归一化、formula paper-context allow 判断和 formula follow-up target 选择，Agent 不再保留对应私有规则函数并降至 4560 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 405 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 paper hint 匹配协议：`contextual_contract_helpers.py` 新增 `paper_from_query_hint`，集中用户文本到论文候选的 title/alias/acronym context 打分逻辑，Agent 仅注入 retriever paper docs 与 candidate lookup，`agent.py` 降至 4514 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 407 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛公式上下文证据判定协议：`contextual_contract_helpers.py` 新增 `paper_context_supports_formula_target`，集中 block text/formula_hint/loss-objective 信号判断，Agent 仅注入 retriever block docs，`agent.py` 降至 4502 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 408 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛论文上下文 scope contract 协议：`contextual_contract_helpers.py` 新增 `paper_scope_correction_contract` 与 `contextual_active_paper_contract`，集中 active paper reference notes、query rewrite 与 contextual metric promotion，Agent 只保留前置条件和 paper lookup 编排，`agent.py` 降至 4465 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 410 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up relationship contract 协议：新增 `followup_relationship_contracts.py`，集中显式“X 是否是 Y 后续工作”方向解析和 `last_followup_relationship` 严格复核 contract 继承，Agent 只注入 target normalization 回调并降至 4399 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 414 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up candidate 小协议：新增 `followup_candidate_helpers.py`，集中候选标题 notes 解析、candidate title/alias 匹配、seed target alias 组装和 domain-aware 候选过滤，solver 与 Agent 直接复用 helper，`agent.py` 降至 4356 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 417 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up relationship payload 协议：`followup_candidate_helpers.py` 接管 follow-up ranking merge、LLM evidence id 白名单过滤和 paper relationship brief payload 构造，Agent 只保留验证器调用与排序编排并降至 4323 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 420 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up relationship validator 协议：`followup_candidate_helpers.py` 接管严格后续验证器 system prompt、human JSON payload 和 LLM JSON 归一化，Agent 只负责触发验证器调用并降至 4264 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 422 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up relationship evidence 协议：`followup_candidate_helpers.py` 接管 seed/candidate 证据过滤、稀疏证据扩展、doc 去重和 candidate-first 排序，Agent 只注入 retriever expansion 回调并降至 4240 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 424 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up 文本规则协议：`followup_candidate_helpers.py` 接管 keyphrase 抽取、follow-up 扩展检索词、fallback reason 和 relation type 推断，Agent 只注入 paper summary 回调并降至 4196 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 427 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up seed/candidate 打分基础规则：`followup_candidate_helpers.py` 接管 paper anchor、paper keyword set、author token 和 seed score 计算，Agent 只注入 active titles 与 paper summary 回调并降至 4132 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 431 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up relationship assessment 规则：`followup_candidate_helpers.py` 接管 direct/strong/weak relationship 打分、主题/作者/phrase overlap 判定和证据不足 reason 追加，Agent 只注入 paper summary 回调并降至 4017 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 432 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up fallback ranking 规则：`followup_candidate_helpers.py` 接管 fallback ranking 的 seed 排除、target/year/keyword/author/relevance boost 和输出 payload 构造，Agent 只注入 paper summary 回调并降至 3947 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 432 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 follow-up LLM selector/ranker 协议：`followup_candidate_helpers.py` 接管 seed selector 与 candidate ranker 的 system prompt、human JSON payload 和 paper brief 构造，Agent 只负责调用 LLM 与后处理并降至 3924 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 435 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 clarification 选择协议：`clarification_intents.py` 接管 selected paper id notes 解析、button/text choice 匹配、pending option 文本选择、ambiguity options notes 回写和 human-choice contract 构造，Agent 保留兼容 wrapper 并降至 3793 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 440 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛消歧裁判协议：`clarification_intents.py` 接管 candidate ranking signals、judge payload/summary、selected option 解析、acronym 展开/归一化与 ambiguity context 匹配规则，Agent 只保留 paper lookup 与 LLM 调用编排并降至 3535 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 445 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 clarification option 标准化协议：`clarification_intents.py` 接管 option schema/index/label/description/source fields/option_id 补全和 contract notes 中 ambiguity options 归一化，Agent 仅保留 `_clarification_options` 兼容入口并降至 3447 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 447 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 clarification session 状态协议：`clarification_intents.py` 接管 clarification tracking key、attempt 计数、pending ambiguity 写入与清理，Agent 保留 agent_loop 兼容 wrapper 并降至 3428 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 449 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 pending clarification contract 协议：`clarification_intents.py` 接管用户 button/text 选择到 QueryContract 的恢复逻辑，Agent 在 contract 提取阶段直接调用 helper 并降至 3401 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 450 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 ambiguity clarification question 协议：`clarification_intents.py` 接管多候选 ambiguity 选项到用户澄清问题的格式化与 pending option fallback，用户可见文案保持原样，Agent 降至 3371 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 451 个测试。
- 2026-04-29 继续按 review §4.2/M1 收敛 Agent emit 协议：`agent_emit.py` 接管 observation/tool_call/agent_step 的 canonical tool 归一化、raw_tool 保留、execution step 写入和 step message 构造，Agent 保留旧方法转发并降至 3361 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 453 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 research runtime 纯规则：`agent_runtime_helpers.py` 接管 excluded focus title 收集、candidate/evidence 标题过滤、entity acronym evidence limit 和 claim focus title 恢复，Agent 保留 wrapper 注入 retriever lookup 并降至 3354 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 455 个测试。
- 2026-04-29 继续按 review §5/M1 收敛 web evidence 工具协议：`web_evidence.py` 接管 web search configured gate、query override、topic/domain 计算和 EvidenceBlock 收集调用，Agent 只注入 web_search client 并降至 3351 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 456 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 citation ranking 候选选择协议：`citation_ranking.py` 接管本地论文 metadata 去重、上一轮推荐《标题》恢复、缺失库内匹配候选保留和推荐 ranker fallback，Agent 仅注入 paper_documents 与 ranker 回调并降至 3306 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 458 个测试。
- 2026-04-29 继续按 review §5/M1 收敛 Semantic Scholar citation evidence 协议：`citation_ranking.py` 接管 Tavily-client gate、Semantic Scholar match API 调用、title overlap 选择、citationCount 解析和 web EvidenceBlock 构造，Agent 仅注入 web_search 与 timeout 并降至 3246 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 460 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 active research 构造协议：`session_context_helpers.py` 接管 ActiveResearch precision fallback、字段填充和 topic signature 初始化，Agent 保留 `_make_active_research` 兼容入口并降至 3243 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 461 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 session history compression 协议：`session_context_helpers.py` 接管压缩窗口选择、system prompt、LLM payload 和 summary/turns 回写，Agent 只负责调用 LLM 与 upsert session 并降至 3224 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 462 个测试。
- 2026-04-29 继续按 review §11/M1 收敛 PDF 渲染子进程安全边界：新增 `pdf_rendering.py` 接管 `pdftoppm` bare-command allowlist、PDF page 渲染命令构造、timeout/DPI 下限、base64 data URL 与 cache 写入，Agent 只保留 VLM figure rendering wrapper 并降至 3174 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 465 个测试。
- 2026-04-29 继续按 review §5/M1 收敛 citation count lookup 协议：`citation_ranking.py` 接管逐候选 web citation lookup 的 tool call payload、Semantic Scholar/web fallback、citation count 提取、web_search event 和 observation payload 组装，Agent 只注入 emit/observation/semantic lookup 回调并降至 3126 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 467 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 memory follow-up answer 协议：新增 `memory_followup_answers.py` 接管记忆综合、上一轮工具 artifact 引用、公式解释追问和中文化修正的 LLM prompt/fallback 文案，Agent 只注入 clients、conversation context 与 OCR 清理回调并降至 3026 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 471 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 LLM memory-followup contract 协议：`conversation_memory_contract.py` 接管会话记忆追问判别器 prompt、conversation_context payload、LLM JSON 归一化和 `memory_followup` QueryContract 构造，Agent 只注入 clients 与上下文回调并降至 2990 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 473 个测试。
- 2026-04-29 继续按 review §4.3/M1 收敛 compound subtask contract 协议：`compound_task_helpers.py` 接管 LLM subtask payload 到 QueryContract 的 relation 推断、默认字段/模态补全、conversation/research mode 修正和同目标字段合并，Agent 只保留兼容 wrapper 与 target normalizer 注入并降至 2849 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 476 个测试。
- 2026-04-29 继续按 review §4.3/M1 收敛 compound LLM decomposer 协议：`compound_task_helpers.py` 接管任务分解器 system prompt、available_tools/conversation_context payload、`invoke_json_messages` history 分支和 `is_compound/subtasks` 顶层解析，Agent 只注入 clients、tool manifest、history 与 target normalizer 并降至 2802 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 478 个测试。
- 2026-04-29 继续按 review §4.3/M1 收敛 compound comparison memory 协议：`compound_task_helpers.py` 接管 comparison 子任务从 `working_memory.target_bindings` 补齐缺失目标、恢复 QueryContract/answer preview 与 memory verification payload，Agent 只保留兼容 wrapper 并降至 2758 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 479 个测试。
- 2026-04-29 继续按 review §4.3/M1 收敛 compound comparison answer 协议：`compound_task_helpers.py` 接管多子任务比较的 comparable payload、LLM 综合 prompt、OCR 清理回调和无 LLM fallback 文案，Agent 只注入 clients/clean_text 并降至 2727 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 480 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 compound intent 上下文判定：`compound_intents.py` 接管 clean query 归一化、target_count 抽取、working_memory target binding 与 active research target 上下文判断，Agent 的 `_should_try_compound_decomposition` 仅保留兼容转发并降至 2717 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 481 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 conversation tool contract 归一化：新增 `conversation_contract_helpers.py` 接管 citation ranking follow-up、公式纠错/解释追问、中文化修正、memory synthesis、library status/recommendation 和通用 conversation relation 的 QueryContract 修正，Agent 只注入 paper hint lookup 并降至 2605 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 487 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 follow-up seed 选择协议：`followup_candidate_helpers.py` 接管后续工作种子论文的 LLM selector 调用、`seed_paper_ids` 归一化和 `followup_seed_score` fallback 排序，Agent 只注入 clients、active_titles 与 paper summary 回调并降至 2578 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 489 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 follow-up candidate pool 扩展协议：`followup_candidate_helpers.py` 接管 seed anchor/expansion terms 查询组装、fresh contract 搜索回调、paper limit 下限和候选去重排序，Agent 只注入 retriever search 与 paper summary 回调并降至 2566 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 490 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 follow-up candidate ranking 协议：`followup_candidate_helpers.py` 接管 seed 排除、显式 candidate_title 直选、LLM ranker payload、关系启发式复核、confidence clamp、fallback ranking merge 和空结果处理，Agent 只注入 selected assessment、clients、confidence 与 paper summary 回调并降至 2498 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 491 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 follow-up validator 调用协议：`followup_candidate_helpers.py` 接管严格后续验证器 LLM 调用、validator human payload 和 assessment 归一化串联，Agent 只注入 clients、paper summary 与 confidence 回调并降至 2487 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 492 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 selected follow-up assessment 协议：`followup_candidate_helpers.py` 接管显式候选论文的关系证据扩展、LLM validator、fallback relationship assessment、证据不足兜底与 evidence id 回填，Agent 只注入 retriever expansion、clients、paper summary 与 confidence 回调并降至 2458 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 493 个测试。
- 2026-04-29 继续按 review §3.1/M1 收敛 clarification question 文案协议：新增 `clarification_question_helpers.py` 接管 ambiguity 优先、公式纠错澄清、LLM 澄清文案 payload、follow-up 无上下文提示和 research goal fallback，Agent 只注入 clients 与 conversation context 并降至 2406 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 497 个测试。
- 2026-04-29 继续按 review §2.2/M1 收敛 selected clarification paper 协议：`agent_runtime_helpers.py` 新增 `prefer_selected_clarification_paper`，统一 search/screen/retry 中 `selected_paper_id` 对候选论文的直选与 lookup fallback，减少三处重复分支并让 Agent 降至 2400 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 498 个测试。
- 2026-04-29 继续按 review M1/M2 收敛 research paper screening 协议：`agent_runtime_helpers.py` 新增 `screen_agent_papers`，接管 selected paper 优先、follow-up candidate filter、formula/figure identity matcher 和 entity evidence precompute/grounding 分支，Agent 只注入 retriever 与 matcher 回调并降至 2377 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 500 个测试。
- 2026-04-29 继续按 review §5/M1 收敛 research evidence search 协议：`agent_runtime_helpers.py` 新增 `search_agent_evidence` 与结构化结果，接管 evidence top_k/query 归一化、concept evidence fallback、precomputed evidence 复用、excluded title 与 selected paper 过滤，Agent 只保留 tool event/observation 写入并降至 2363 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 502 个测试。
- 2026-04-29 继续按 review §5/M1 收敛 web evidence 工具状态协议：`web_evidence.py` 新增 `search_agent_web_evidence` 与本地 `max_results` 约束，接管 web query override/default、result limit clamp、web evidence 收集和本地/网页证据 merge；Agent 保留 tool event 与 observation 写入。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 504 个测试。
- 2026-04-29 继续按 review §5/M1 收敛 candidate paper search 协议：`agent_runtime_helpers.py` 新增 `search_agent_candidate_papers`，接管候选论文检索、excluded title 过滤、follow-up active target fallback 和 selected paper lookup 直选；Agent 保留 search_corpus tool_call 时序、screening 和 observation 写入并降至 2355 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 506 个测试。
- 2026-04-29 继续按 review §5/M1 收敛 web research claim 追加协议：`web_evidence.py` 新增 `claims_with_web_research_claim`，统一 auto-resolve 与普通 solver 路径的 web evidence claim 追加判断，Agent 只注入现有 `_build_web_research_claim` 兼容 builder。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 507 个测试。
- 2026-04-29 继续按 review M1/M2 收敛 verification 状态转换协议：`agent_runtime_helpers.py` 新增 `clarify_retry_verification_if_needed`，统一 targeted definition/mechanism/figure/general answer 在 retry 后转人工澄清的规则，Agent 只保留 verify tool event 与 retry 调用编排。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 508 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 retry research materials 协议：`agent_runtime_helpers.py` 新增 `retry_research_limits` 与 `prepare_retry_research_materials`，接管 retry paper/evidence limit 扩展、候选重搜、excluded title/selected paper 过滤、concept/entity/expand 三路证据检索和 entity grounding；Agent 只保留 retry tool event、solver/verification 调用与 state 写回并降至 2337 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 510 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 disambiguation judge 协议：`clarification_intents.py` 接管 LLM judge 自动消歧阈值判断、推荐候选/拒绝理由标注排序，以及 auto-resolve 后 `selected_ambiguity_option/selected_paper_id/disambiguation_judge_*` notes 写回；Agent 只保留 LLM judge 调用和状态刷新编排并降至 2267 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 511 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 evidence disambiguation 判定协议：`clarification_intents.py` 新增 `contract_needs_evidence_disambiguation`，集中短 acronym、`ambiguous_slot` notes 与 disambiguation goal 判断，Agent 删除本地判定方法并降至 2256 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 512 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 acronym option 聚合协议：`clarification_intents.py` 新增 `acronym_options_from_evidence`，接管 EvidenceBlock 到 acronym meaning candidate 的分桶、expansion 优先、plain acronym duplicate 删除、context_text 填充与排序；Agent 只注入 paper lookup 并降至 2203 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 513 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 acronym corpus fallback 协议：`clarification_intents.py` 新增 `acronym_evidence_from_corpus`，接管 paper/block documents 扫描、target 匹配、expansion/formula_hint 评分和 EvidenceBlock 构造；Agent 只注入 retriever document 访问器并降至 2176 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 514 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 acronym disambiguation option finalize 协议：`clarification_intents.py` 新增 `finalize_acronym_disambiguation_options`，接管 excluded title 过滤、context target 匹配、候选数量 gate 与 `normalize_clarification_options` 调用；Agent 的 evidence disambiguation 编排降至 2158 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 515 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 selected ambiguity refresh 协议：`agent_runtime_helpers.py` 新增 `refresh_selected_ambiguity_materials`，接管 auto-resolved paper 的 candidate pool 选择、已有 evidence 复用、缺失 evidence 的 concept/expand 补检索和 excluded title 过滤；Agent 只保留 screen/evidence SSE 与 observation 写入并降至 2149 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 517 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 disambiguation judge prompt 协议：`clarification_intents.py` 新增 `disambiguation_judge_system_prompt` 与 `disambiguation_judge_human_prompt`，接管 LLM 消歧裁判器 prompt、QueryContract payload、notes 过滤、candidate option payload 和 output schema；Agent 只保留 clients 调用与返回校验并降至 2112 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 518 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 solve claims + web claim 协议：`web_evidence.py` 新增 `solve_claims_with_web_research`，统一 auto-resolve 与普通 solver 路径中的 solver 调用和 web research claim 追加，Agent 只注入 `_run_solvers` 与 `_build_web_research_claim` 回调并降至 2108 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 519 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 evidence disambiguation 编排协议：`clarification_intents.py` 新增 `evidence_disambiguation_options`，集中 resolved/selected/target-binding 短路、公式宽搜与 corpus fallback、最终 option 标准化；Agent 只注入 retriever/source callback 并降至 2100 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 523 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 clarification limit best-effort 协议：`agent_runtime_helpers.py` 新增 `clarification_limit_decision` 与 `promote_best_effort_state_after_clarification_limit`，集中澄清上限命中后的默认假设 contract、forced plan、observation payload 和 best-effort verification 提升；Agent 只保留事件记录与 runtime 调用并降至 2059 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 527 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 agent reflection 决策协议：`agent_runtime_helpers.py` 新增 `reflect_agent_state_decision`，集中重复命中用户排除焦点、已有 clarify verification 透传、acronym 多义未解决澄清和 pass/retry reason 生成；Agent 只注入 focus title、memory binding 和 ambiguity count 回调并降至 2033 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 531 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 disambiguation judge resolution 协议：`clarification_intents.py` 新增 `resolve_disambiguation_judge_decision`，统一 judge 后自动消歧/人工澄清分流、observation payload、推荐候选标注、clarify verification 和 contract notes 写回；Agent 只保留 LLM judge 调用、刷新证据与 solver 编排并降至 2007 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 533 个测试。
- 2026-04-30 继续按 review M1 清理 Agent 私有转发壳：删除无调用点的 `_filter_candidate_papers_by_excluded_titles`、`_filter_evidence_by_excluded_titles`、`_entity_evidence_limit` 和仅测试使用的 `_contract_with_ambiguity_options`，测试改为直接复用 `clarification_intents.contract_with_ambiguity_options`，`agent.py` 降至 1980 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 533 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 candidate paper search 编排协议：`agent_runtime_helpers.py` 新增 `run_agent_paper_search`，集中 `search_corpus` 的 query/top_k 归一化、候选搜索、screen 回调、tool_call 参数和 observation payload 构造；Agent 只保留 retriever 注入、SSE emit 和 state 写回并降至 1955 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 534 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 evidence search payload 协议：`search_agent_evidence` 现在返回 `tool_call_arguments / observation_summary / observation_payload`，集中 evidence query/top_k/paper_ids/modality 与 block type 摘要构造；Agent 只负责 emit evidence 与写 state 并降至 1941 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 534 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 web evidence payload 协议：`search_agent_web_evidence` 现在返回 `tool_call_arguments / observation_summary / observation_payload`，统一网页检索 query/max_results/enabled 与 web evidence 计数上报；Agent 只负责 emit web/evidence 与写 state 并降至 1933 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 534 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 claims solver 状态协议：`agent_runtime_helpers.py` 新增 `solve_agent_state_claims`，统一从当前 state 读取 contract/plan/papers/evidence/web_evidence 并调用 solver 与 web claim append；Agent 的 ambiguity auto-resolve 与普通 compose 路径共用该入口并降至 1928 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 535 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 retry verification 协议：`agent_runtime_helpers.py` 新增 `run_retry_verification_from_materials` 与 `RetryVerificationResult`，集中 retry plan 归零、solver/verify 调用、pass 时材料替换、figure identity 偏向和 retry observation payload；Agent 只保留 retry tool_call、材料准备和 state 写回并降至 1923 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 537 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 verify grounding 事件协议：`agent_runtime_helpers.py` 新增 `verify_grounding_tool_call_arguments` 与 `verification_observation_payload`，统一 verify_claim tool_call 参数和 verification observation payload 构造，减少 Agent 内事件字段散落。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 538 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 research intent markers：`research_intents.py` 将 origin/metric/summary/answer-slot/web-search markers 集中为 marker profiles，并新增 `query_matches_any` 作为统一匹配入口，减少内联规则散落，为后续下沉到配置或 LLM router 留出边界。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 539 个测试。
- 2026-04-30 继续按 review §3.1/H4 收敛 library intent markers：新增 `intent_marker_matching.py` 作为通用 query normalize/match 入口，`library_intents.py` 将库状态、库推荐、引用排序等 markers 集中为 `LIBRARY_INTENT_MARKERS` profiles，`research_intents.py` 复用同一匹配入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 540 个测试。
- 2026-04-30 继续按 review §3.1/M2 收敛 compound intent markers：`compound_intents.py` 将库内 count+推荐、比较、复合任务 cues 集中为 `COMPOUND_INTENT_MARKERS` profiles，并复用 `intent_marker_matching.query_matches_any`，保持复合拆解启发式行为不变但减少内联规则。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 541 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 memory intent markers：`memory_intents.py` 将 pdf-agent 拓扑、会话比较、上一轮引用、短追问、最近工具结果和序数引用 markers 集中为 `MEMORY_INTENT_MARKERS` profiles，并复用通用 matcher，保持会话追问绑定行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 542 个测试。
- 2026-04-30 继续按 review §3.1/H7-H8 收敛 follow-up/correction intent markers：`followup_intents.py` 将公式解释、语言偏好、当前论文指代、公式纠错、论文范围纠错、上下文 metric、公式位置纠错和负反馈 markers 集中为 `FOLLOWUP_INTENT_MARKERS` profiles，并统一原文/lowered/compact 三路匹配入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 543 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 follow-up relationship markers：`followup_relationship_intents.py` 将 strict recheck、soft/support relation、target-window、domain 和 seed-intro cues 归入 `FOLLOWUP_RELATIONSHIP_MARKERS` profiles，并复用通用 matcher，保持后续论文关系验证启发式不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 544 个测试。
- 2026-04-30 继续按 review §2.2/§3.1 收敛 clarification choice markers：`clarification_intents.py` 将澄清选择 markers 与 ordinal patterns 类型化为 marker profiles，并通过 `CLARIFICATION_INTENT_MARKERS`/通用 matcher 统一选择文本识别入口，保持 pending clarification 选择协议不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 544 个测试。
- 2026-04-30 继续按 review §3.2/M1 收敛 solver goal markers：`solver_goal_helpers.py` 新增 `SOLVER_GOAL_MARKERS`，复用 `research_intents` 的 origin/answer-slot marker profiles 并保留 solver 专属补充词，fallback goal 推断统一走通用 matcher，减少 goal/solver 边界重复规则。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/H10 收敛 intent router markers：`intent.py` 新增 `INTENT_ROUTER_MARKERS`，集中 contextual origin refine、self-identity、capability 与 previous-rationale 小路由 cues，旧 protected routing 行为不变但减少内联判断。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.2/M1 收敛 retrieval goal markers：`retrieval.py` 的 `_contract_goals` 复用 `SOLVER_GOAL_MARKERS` 和 `looks_like_metric_goal`，保留 definition 需要 targets 的 retrieval 语义，同时减少检索排序与 solver fallback 的重复词表。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 topology recommendation markers：`topology_recommendation_helpers.py` 将不可用推荐文案的否定 markers 提升为 `UNUSABLE_TOPOLOGY_RECOMMENDATION_MARKERS` profile，并复用通用 matcher，保持 topology recommendation fallback 判断不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 figure intent markers：`figure_intents.py` 将 figure signal、显式 figure reference 与 benchmark aliases 类型化为 marker profiles，并复用通用 matcher，保持 figure 检索/回答信号判断不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 contextual contract markers：`contextual_contract_helpers.py` 将公式上下文支持 cues 集中为 `CONTEXTUAL_CONTRACT_MARKERS` profile，并复用通用 matcher，保持 active paper/formula context 绑定前的保守判断不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 formula text markers：`formula_text_helpers.py` 新增 `FORMULA_TEXT_MARKERS` profiles，集中公式窗口打分、LaTeX 检测、objective/gradient 偏置、appendix 噪声和梯度查询 cues，并复用通用 matcher，保持公式抽取/排序行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §5/M1 收敛 web evidence markers：`web_evidence.py` 新增 `WEB_EVIDENCE_MARKERS` profiles，集中 news topic 与 research-domain include-domain cues，并复用通用 matcher，保持 web search topic/domain 选择行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 verifier markers：`claim_verifier.py` 新增 `CLAIM_VERIFIER_MARKERS` profiles，metric verification 复用 `looks_like_metric_goal`，公式 claim/evidence 支持判断统一走通用 matcher，保持 verifier 行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 library composer markers：`answer_composer.py` 新增 `LIBRARY_COMPOSER_MARKERS` profiles，集中库状态推荐/列表、重复推荐 same-best、recent/survey 排序偏好 cues，并复用通用 matcher，保持库状态与库推荐回答行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 follow-up routing markers：`followup_routing.py` 新增 `FOLLOWUP_ROUTING_MARKERS` profiles，集中公式追问、细节追问、短模糊追问和上下文质疑 cues，并复用通用 matcher，保持 follow-up contract refinement 行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 follow-up candidate markers：`followup_candidate_helpers.py` 新增 `FOLLOWUP_CANDIDATE_MARKERS` profiles，集中严格直接使用、dataset/benchmark、transfer 和 method/model extension 关系类型 cues，并复用通用 matcher，保持候选后续论文关系标签行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 545 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 answer composer markers：`answer_composer.py` 新增 `ANSWER_COMPOSER_MARKERS` profiles，集中 topology fallback 阻塞文案和公式变量数学符号 cues，并新增静态单测覆盖，保持拓扑建议清洗与公式变量格式化行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 547 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 retrieval markers：`retrieval.py` 新增 `RETRIEVAL_MARKERS` profiles，集中 figure 1、机制/公式/应用检索加权和公式噪声扣分 cues，并新增静态评分单测覆盖，保持检索排序权重与过滤行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 548 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 entity definition type markers：`entity_definition.py` 新增 `ENTITY_DEFINITION_MARKERS` profiles，集中算法/数据集/框架/模型类型判定 cues，并新增 marker 单测覆盖，保持实体类型 fallback 归类行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 549 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 entity definition explanation markers：`entity_definition.py` 扩展 `ENTITY_DEFINITION_MARKERS` profiles，集中 GRPO/entity 介绍句、机制细节、流程步骤和 summary bullet cues，并保持 summary 使用窄词表避免扩大触发面。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 550 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 concept reasoning markers：`concept_reasoning.py` 新增 `CONCEPT_REASONING_MARKERS` profiles，集中概念分类与 RLHF/policy optimization/RAG 详情 cues，并新增 marker 单测覆盖，保持 concept definition fallback 文案行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 552 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 solver pipeline origin markers：`solver_pipeline.py` 新增 `SOLVER_PIPELINE_MARKERS` profile，集中 origin solver 对 introduce/propose 证据的加权 cue，并新增 marker 单测覆盖，保持起源论文选择排序行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 553 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 Zotero ingestion markers：`zotero_sqlite.py` 新增 `PAPERLIKE_WEBPAGE_TITLE_MARKERS` profile，集中网页条目转论文候选的 title cues，并新增 reader 回归测试覆盖，保持 Zotero webpage include 判定行为不变。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 554 个测试。
- 2026-04-30 继续按 review §4.2/M1 收敛 SSE 事件协议：`agent_events.py` 显式规范化 `thinking_delta` 与 `ask_human` 事件，补齐 question/options/reason 与 text 字段归一化，并新增事件协议测试覆盖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 555 个测试。
- 2026-04-30 继续按 review §4.3/M2 收敛 Task tool-use loop：`Task` 现在进入 research executable tool set，`build_research_tool_registry` 可在研究 loop 中执行子任务并记录 task result observation，新增 research runtime Task 测试覆盖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 556 个测试。
- 2026-04-30 继续按 review §10/M3 收敛 trace diff 可观测性：`agent_trace_diff.py` 将 `ask_human` question 与 options_count 纳入稳定签名，trace diff 现在能捕捉澄清问题/选项数量变化，并新增回归测试覆盖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 557 个测试。
- 2026-04-30 按 review 收口当前重构状态：新增 `docs/review-2026-04-30-progress.md`，记录已完成的 M1/M2 安全切片、当前 `agent.py` 约 1925 行/557 测试基线，以及剩余需要明确架构确认的高风险项（完全删除 QueryContract 控制流、IntentRecognizer fallback、solver pipeline、动态工具执行沙箱、默认 logprobs/self-consistency 与 legacy alias）。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 557 个测试。
- 2026-04-30 继续按 review M1/M2 收敛指标追问路由：`followup_intents.py` 新增 metric-definition follow-up profile，`conversation_contract_helpers.py` 在已有 metric/table 上下文中将“这个准确度/accuracy 是怎么定义或计算的”恢复为 research `metric_value_lookup`，避免误落到 memory follow-up 或实体定义回答；contextual follow-up LLM prompt 同步约束 metric definition 必须检索 table/caption/page_text 证据。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 559 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 metric-definition 计划语义：`research_planning.py` 将 `metric_definition` requested field 显式映射为 `metric_value + setting + definition` goals，让指标定义追问同时触发 text/table solver 和文本定义证据检索，而不是只读表格数值。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 560 个测试。
- 2026-04-30 继续按 review M1/M2 补指标定义追问端到端回归：新增 `_extract_query_contract` 测试，覆盖“上一轮问 ICA/PBA 准确度表格，下一轮问这个准确度怎么定义”的真实上下文，确保最终 contract 为 research `metric_value_lookup`、继承 active targets，并携带 `metric_definition` requested field。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 561 个测试。
- 2026-04-30 按 review §6/M3 补动态工具沙箱边界文档：新增 `docs/tool-sandbox.md`，明确 `propose_tool` 当前只写 pending proposal、不执行代码，并定义后续 approval stages、隔离进程、资源限制、网络/文件 deny-by-default、schema 校验、审计日志和 runtime registration 元数据要求，避免在未实现沙箱前误开启动态工具执行。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 561 个测试。
- 2026-04-30 继续按 review §6/M3 收敛动态工具审计字段：`proposed_tools.py` 在 pending proposal payload 中加入 `proposal_id` 与完整 `code_sha256`，让后续 admin approval、sandbox test 与 runtime registration 可以绑定稳定代码哈希；`docs/tool-sandbox.md` 同步记录当前审计字段。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 561 个测试。
- 2026-04-30 继续按 review §4.2/M1 收敛 SSE 事件协议：`agent_events.py` 将 `todo_update` 纳入 canonical event type，并复用 todo item normalizer 输出稳定 `{id,text,status}` items，前端可直接按协议渲染 `todo_write` 的可见任务列表。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 562 个测试。
- 2026-04-30 继续按 review §10/M3 收敛 trace diff 可观测性：`agent_trace_diff.py` 将 `todo_update` 的 todo items 纳入稳定签名，trace 回归现在能捕捉可见任务列表的文本/状态变化。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 563 个测试。
- 2026-04-30 继续按 review §4.2/M1 收敛最终 SSE 事件协议：`agent_events.py` 为 `final` 事件补齐 `type=final`、`answer`、`citations`、`usage` 默认字段，`astream_chat_events` 的最终 payload 现在也走统一归一化层。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 564 个测试。
- 2026-04-30 继续按 review §4.2/M1 收敛异常 SSE 事件协议：`routes.py` 的 stream exception 分支改为通过 `_stream_error_events` 统一输出 normalized `error` 与 `final` payload，避免异常路径绕过 canonical event schema；新增 API route 单测覆盖 SSE JSON 格式与 error/final 默认字段。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 566 个测试。
- 2026-04-30 更新 review 收口快照：`docs/review-2026-04-30-progress.md` 的 baseline 更新到 `481e34a`、`agent.py` 约 1929 行与 566 测试，并补记 metric-definition follow-up、tool sandbox 文档/审计字段、todo/final/error event protocol 与 trace diff 收口状态。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 566 个测试。
- 2026-04-30 以 `rollback-review-high-risk-start-2026-04-30` 标签标记高风险架构迁移回滚线并推送到远程；随后按 review §8/M2 启用 `LLMIntentRouter` 作为主路由入口，新增 `query_contract_from_router_decision` 将 router tool-call 决策转换成兼容 `QueryContract`，router 不可用时才回退旧 `IntentRecognizer`。新增端到端测试确认 `_extract_query_contract` 优先使用 LLM tool router 而不是旧 marker protected path。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 569 个测试。
- 2026-04-30 继续按 review §8/M2 解耦 QueryContract 兼容适配层：新增 `intent_contract_adapter.py` 承接 research slot profile、relation 与 requested field/modality/answer shape/precision 映射，`intent_router.py` 不再依赖旧 `IntentRecognizer` 私有静态方法，旧 recognizer 只保留 fallback/compatibility wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 571 个测试。
- 2026-04-30 继续按 review §8/M2 解耦 legacy intent fallback：`query_shaping.py` 新增 `fallback_query_targets` 与 `fallback_target_aliases`，承接旧 `IntentRecognizer` 私有 target 抽取和 loss notation alias 生成；`IntentRecognizer` 不再保存这段私有实现，后续 LLM router/compatibility fallback 可复用同一 target shaping 入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 573 个测试。
- 2026-04-30 继续按 review §8/M2 强化 LLM tool router 兼容层：`query_contract_from_router_decision` 在 tool-call args 未提供 targets、且预抽取目标为空时，会复用 `fallback_query_targets` 从 router query 中补出候选目标，减少新 router 因漏填结构化 targets 而退回旧 `IntentRecognizer` 行为的概率。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 574 个测试。
- 2026-04-30 继续按 review §8/M2 解耦 legacy router payload adapter：`intent_contract_adapter.py` 新增 `RELATION_ANSWER_SLOTS` 与 `answer_slots_from_relation`，承接旧 relation 到 answer_slots 的兼容映射；`IntentRecognizer._intent_from_legacy_contract_payload` 不再内嵌 20+ 项映射表。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 575 个测试。
- 2026-04-30 继续按 review §8/M2 解耦 protected conversation intent：新增 `conversation_intents.py` 承接空输入、寒暄、自我介绍、能力说明和过短澄清的本地保护意图；`IntentRecognizer._protected_local_intent` 只负责将 helper payload 包装为 `Intent`，减少旧 recognizer 内的对话硬编码。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 577 个测试。
- 2026-04-30 继续按 review §8/M2 清理 legacy recognizer 转发壳：`IntentRecognizer` 删除 smalltalk relation、research relation/requirements、memory follow-up、web-search 与 research slot 的静态 wrapper，改为直接调用 `conversation_intents`、`intent_contract_adapter`、`memory_intents` 和 `research_intents` helper；旧 recognizer 进一步收缩为 LLM 调用 + legacy payload/fallback 兼容层。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 578 个测试。
- 2026-04-30 继续按 review §8/M2 解耦 legacy fallback 分支：新增 `intent_fallback_helpers.py`，将库状态、库推荐、引用排序、记忆比较和上一轮依据追问的非研究 fallback payload 从 `IntentRecognizer._fallback_intent` 迁出；旧 recognizer fallback 只保留研究路径兜底组装。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 580 个测试。
- 2026-04-30 继续按 review §8/M2 解耦 research fallback payload：`intent_fallback_helpers.py` 新增 `research_fallback_intent`，集中 research/memory fallback 的 slot 推断、active targets 继承、web-search flag 和 target_aliases 生成；`IntentRecognizer._fallback_intent` 现在仅负责计算上下文并调用 fallback helper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 581 个测试。
- 2026-04-30 继续按 review §8/M2 解耦 legacy LLM intent prompt：新增 `intent_llm_prompt.py` 承接旧 `IntentRecognizer._llm_intent` 的结构化意图 system prompt，旧 recognizer 不再内嵌长 prompt 字符串，为后续替换为 tool-use router/config prompt 留出边界。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 582 个测试。
- 2026-04-30 继续按 review §8/M2 解耦 legacy contract payload adapter：新增 `intent_legacy_adapter.py`，将旧 LLM relation-style payload 到 fallback intent payload 的兼容转换从 `IntentRecognizer._intent_from_legacy_contract_payload` 迁出，保留 origin/refine/memory 兼容语义并新增单测覆盖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 585 个测试。
- 2026-04-30 更新 review 收口快照：`docs/review-2026-04-30-progress.md` 的 baseline 更新到 `9c08c81`、`agent.py` 约 1964 行、`intent.py` 约 510 行与 585 测试，并补记高风险回滚 tag、LLM router 优先入口和 legacy intent recognizer 拆分状态。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 585 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent 兼容转发壳：删除仅测试使用的 `ResearchAssistantAgentV4._canonical_tools`，测试改为直接覆盖 `contract_context.canonical_tools` + `LEGACY_TOOL_NAME_ALIASES`，减少 Agent 对 legacy alias helper 的无生产调用依赖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 585 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent dead wrapper：删除无调用点的 `_compose_formula_interpretation_followup_answer` 与 `_compose_language_preference_followup_answer`，保留 `memory_followup_answers.py` 中已直接覆盖的实现，进一步减少 Agent 对细分 memory follow-up composer 的转发壳。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 585 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent memory wrapper：删除无生产调用的 `_llm_memory_followup_contract`，保留 `conversation_memory_contract.llm_memory_followup_contract` 的直接测试覆盖，进一步减少 Agent 对 memory contract helper 的转发层。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 585 个测试。
- 2026-04-30 继续按 review §4.3/M1 清理 compound Agent 转发壳：`agent_compound.py` 直接调用 `merge_redundant_field_subtasks` 与 `compose_compound_comparison_answer` helper，删除 Agent 内 `_merge_redundant_field_subtasks`、`_compose_compound_comparison_answer`、`_comparison_results_with_memory` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 585 个测试。
- 2026-04-30 继续按 review §4.3/M1 清理 compound 子任务解析转发壳：删除无生产调用的 `_subtask_contract_from_payload` 与 `_subtask_relation_from_slots`，保留 `compound_task_helpers.py` 中直接覆盖的 relation/contract 解析测试。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 585 个测试。
- 2026-04-30 继续按 review §4.3/M1 收敛 compound planner 调用边界：`agent_compound.py` 直接调用 `should_try_compound_decomposition`、`llm_decompose_compound_query` 与 tool manifest/target normalizer，删除 Agent 内 `_should_try_compound_decomposition` 和 `_llm_decompose_compound_query` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 585 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 citation registry 调用边界：`agent_tool_registries.py` 直接调用 `select_citation_ranking_candidates`、`lookup_candidate_citation_counts` 与 `semantic_scholar_citation_evidence`，删除 Agent 内 citation ranking 三个转发方法，减少 conversation tool registry 对 Agent 壳方法的依赖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 585 个测试。
- 2026-04-30 更新 review 收口快照：`docs/review-2026-04-30-progress.md` 的 baseline 更新到 `b46601d`、`agent.py` 约 1758 行，并补记 compound planner/helper 直连和 citation registry/helper 直连后的 Agent compatibility shell 清理状态。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 585 个测试。
- 2026-04-30 继续按 review §7/M2 推进 logprobs confidence 底座：`confidence.py` 新增 `confidence_from_logprobs`，将 token logprob 序列标准化为 `Confidence(basis=\"logprobs\")` payload，覆盖平均/最小 logprob、token_count 与缺失信号场景；后续 provider streaming 可直接接入该入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 587 个测试。
- 2026-04-30 继续按 review §7/M2 推进 provider logprobs 采集接口：`ModelClients.stream_text` 增加默认关闭的 `request_logprobs/on_logprobs` 参数，并新增 `_extract_chunk_logprobs` 从 LangChain/OpenAI chunk metadata 中抽取真实 token logprob，避免把 `top_logprobs` 候选项误计入 confidence。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 589 个测试。
- 2026-04-30 继续按 review §7/M2 推进 answer confidence 接线：`AgentSettings` 新增默认关闭的 answer logprobs 开关，`AnswerComposerMixin` 可选透传 `request_logprobs/on_logprobs`，研究流式回答会在开启后把 `Confidence(basis="logprobs")` 写入 `confidence` 事件、agent state 与 runtime summary。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 591 个测试。
- 2026-04-30 继续按 review §8/M2 加固 LLM router miss 护栏：`LLMIntentRouter` 将无效 tool-plan payload 标记为 `router_invalid_payload`，同时保持 `router_unavailable` 回退语义；新增回归测试确认坏 router payload 不污染 `QueryContract`，旧 deterministic fallback 仍接管。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 593 个测试。
- 2026-04-30 继续按 review §10/M3 加固 trace diff 可观测性：`agent_trace_diff.py` 将 contract 的 interaction_mode/relation/intent_kind/router_action/router_tags 与 confidence score bucket 纳入稳定签名，后续替换 router/composer/solver 时可捕捉路由和置信度漂移。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 595 个测试。
- 2026-04-30 继续按 review §8/M2 为删除 legacy intent fallback 铺路：`AgentSettings` 新增默认开启的 `legacy_intent_fallback_enabled` 兼容开关；关闭后 LLM router miss 会返回明确的 `clarify_user_intent` contract，而不是继续走旧 `IntentRecognizer` deterministic fallback，便于后续灰度迁移。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 更新 review 收口快照：`docs/review-2026-04-30-progress.md` 的 baseline 更新到 `b8363e1`、`agent.py` 约 1787 行与 596 测试，并补记 answer logprobs、router miss/fallback gate 和 trace drift 签名收口状态。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent 兼容转发壳：删除无生产调用的 `_llm_validate_followup_candidate` wrapper，保留 `followup_candidate_helpers.llm_validate_followup_candidate` 的直接单测覆盖，减少 Agent 对 follow-up validator helper 的空转发依赖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent clarification 静态转发壳：删除仅测试使用的 `_contract_from_selected_clarification_option`，测试改为直接覆盖 `clarification_intents.contract_from_selected_clarification_option`，减少 Agent 对 clarification helper 的兼容入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent figure 静态转发壳：删除仅测试使用的 `_build_figure_contexts` 与 `_figure_signal_score`，测试改为直接覆盖 `evidence_presentation.build_figure_contexts` 和 `figure_intents.figure_signal_score`，减少 Agent 对 figure helper 的空包装。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review §4.3/M1 收敛 compound Task 调用边界：`agent_compound.py` 直接调用统一 `_execute_compound_task_subagent`，删除 conversation/research 两个同构 compound subtask wrapper，测试 monkeypatch 同步切到统一 Task 入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 memory answer 调用边界：`agent_tool_registries.py` 直接调用 `memory_followup_answers` helper，删除 Agent 内 `_compose_memory_followup_answer` 与 `_compose_memory_synthesis_answer` wrapper，测试改为直接覆盖 helper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review §4.2/M1 收敛 agent step emit 边界：`agent_runtime_helpers.execute_tool_loop` 直接调用 `agent_emit.emit_agent_step`，删除 Agent 内 `_emit_agent_step` wrapper，相关测试改为断言真实 `agent_step` 事件的 action/arguments。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 clarification tracking 边界：在澄清上限与澄清尝试记忆处直接调用 `clarification_tracking_key/next_clarification_attempt` helper，删除 `_next_clarification_attempt` 与 `_clarification_key` 薄包装。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 收敛 paper recommendation helper 边界：`solver_pipeline.py` 在推荐 claim 构造时直接调用 `evidence_presentation.paper_recommendation_reason`，删除 Agent 内 `_paper_recommendation_reason` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review §3.1/M1 收敛 follow-up candidate helper 边界：`_rank_followup_candidates` 直接调用 `selected_followup_candidate_assessment` helper 并注入 retriever/LLM 回调，删除 Agent 内 `_selected_followup_candidate_assessment` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 更新 review 收口快照：`docs/review-2026-04-30-progress.md` 的 baseline 更新到 `e12b68a`、`agent.py` 约 1638 行与 596 测试，并补记 memory/follow-up/clarification/figure/compound/agent-step/recommendation shell 清理状态。使用 `zotero-paper-rag` conda 环境全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 persistent learning 转发壳：`_session_conversation_context` 直接调用 `load_learnings` 并保留原有异常日志/空上下文 fallback，删除 Agent 内 `_persistent_learnings_context` wrapper，使会话上下文边界更直接。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 session history 转发壳：planner、compound planner、research composer 与 follow-up refinement 直接调用 `session_context_helpers.session_llm_history_messages`，删除 Agent 内 `_session_llm_history_messages` wrapper，保持历史消息渲染 helper 的既有语义。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 web evidence 转发壳：`_agent_web_search` 和 claims assembly 直接调用 `web_evidence.collect_web_evidence/build_web_research_claim`，删除 Agent 内 `_collect_web_evidence` 与 `_build_web_research_claim` wrapper，测试同步改为覆盖 helper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 web-search 开关转发壳：standard turn loop 直接调用 `query_shaping.should_use_web_search`，删除 Agent 内 `_should_use_web_search` wrapper，并移除测试假对象里已失效的覆盖入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 session/clarification 转发壳：turn loop 与 compound loop 直接调用 `clarification_options_from_contract_notes`、`clear_pending_clarification`、`reset_clarification_tracking` 和 `make_active_research`，删除 Agent 内 `_clarification_options`、`_clear_pending_clarification`、`_reset_clarification_tracking`、`_make_active_research` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 research plan 转发壳：`AgentRuntime.run_research_agent_loop` 直接调用 `research_planning.build_research_plan(contract, settings)`，删除 Agent 内 `_build_research_plan` wrapper，测试改为直接覆盖 research planning helper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 excluded focus 转发壳：runtime 与 disambiguation 路径直接调用 `agent_runtime_helpers.excluded_focus_titles`，删除 Agent 内 `_excluded_focus_titles` wrapper，减少 runtime 对 Agent helper 壳的依赖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 target normalization 转发壳：IntentRecognizer、LLM router adapter、follow-up routing 与 compound planner 直接调用 `contract_normalization.normalize_contract_targets` 并注入 retriever canonicalizer，删除 Agent 内 `_normalize_contract_targets` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 runtime summary 转发壳：standard turn 与 compound turn loop 直接调用 `agent_runtime_summary.build_runtime_summary`，删除 Agent 内 `_runtime_summary` wrapper，测试断言切到真实 summary 的 `answer_generation.confidence` 字段。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 citation resolution 转发壳：research composer、conversation registry 和测试直接调用 `evidence_presentation.citations_from_doc_ids` 并注入 retriever lookups，删除 Agent 内 `_citations_from_doc_ids` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 claim focus 转发壳：research turn 与 reflection 路径直接调用 `agent_runtime_helpers.claim_focus_titles` 并注入 paper title lookup，删除 Agent 内 `_claim_focus_titles` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 clarification tracking 转发壳：standard turn 与 compound turn loop 直接调用 `store_pending_clarification`、`clarification_tracking_key` 和 `remember_clarification_attempt`，删除 Agent 内 `_store_pending_clarification` 与 `_remember_clarification_attempt` wrapper，并让测试检查真实 session tracking。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 identity paper filter 转发壳：screening 与 retry helper 的 `prefer_identity_matching_papers` 回调直接使用 `_paper_identity_matches_targets` 过滤候选论文，删除 Agent 内 `_prefer_identity_matching_papers` wrapper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review M1/M2 清理 planner 转发壳：standard turn loop 与 Task subagent 直接调用 `AgentPlanner.plan_actions`，删除 Agent 内 `_plan_agent_actions` wrapper，并补齐测试替身的 planner stub。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 596 个测试。
- 2026-04-30 继续按 review §8/M2 推进 legacy fallback 灰度移除：以 `rollback-legacy-fallback-start-2026-04-30` 标记当前回滚线；`LLMIntentRouter` 的 `answer_directly` contract adapter 开始保留 `answer_style` 子类型（`greeting`/`self_identity`/`capability`/general），避免关闭旧 `IntentRecognizer` fallback 后受保护对话意图全部退化为 `general_question`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 597 个测试。
- 2026-04-30 继续按 review §8/M2 扩展 LLM router contract schema：新增 `need_conversation_tool` action 覆盖库状态、库推荐、引用排序和记忆追问等本地对话工具；`need_corpus_search` 可显式携带 relation、requested fields、modalities、answer shape 与 precision hints，减少 fallback-off 路径对旧 `IntentRecognizer` 重新推断 relation 的依赖。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 599 个测试。
- 2026-04-30 继续按 review §8/M2 将 legacy intent fallback 默认关闭：`AgentSettings` 与 `Settings` 的 `agent_legacy_intent_fallback_enabled` 默认值切到 `False`；测试替身补齐 `invoke_tool_plan_messages`，用真实 tool-plan payload 覆盖库状态/推荐、记忆追问、起源修复、指标/公式追问和后续工作等主链路。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 599 个测试。
- 2026-04-30 继续按 review §8/M2 切断 runtime legacy intent fallback：`ResearchAssistantAgentV4` 不再实例化或调用 `IntentRecognizer`，router miss 统一返回明确澄清合同；同时删除 `agent_legacy_intent_fallback_enabled` 运行时配置项，避免默认关闭后仍保留可误用的旧规则入口。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 599 个测试。
- 2026-04-30 继续按 review §8/M2 物理删除 legacy intent fallback：移除 `app/services/intent.py`、`intent_fallback_helpers.py`、`intent_legacy_adapter.py`、`intent_llm_prompt.py` 及仅覆盖这些死路径的测试，项目现在只保留 `LLMIntentRouter` + typed adapters 的主路由栈。使用 `zotero-paper-rag` conda 环境编译检查与全量 `pytest` 通过 592 个测试。
- 2026-04-30 继续按 review §6/M3 收敛动态工具审批状态机：`proposed_tools.py` 新增 proposal status transition API，强制 code hash 匹配、禁止从 `pending_review` 直接跳到 runtime approval，并要求 `approved_for_runtime` 附带 sandbox report；`docs/tool-sandbox.md` 同步记录该门禁。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 594 个测试。
- 2026-04-30 继续按 review §5/M2 清理检索工具 legacy schema：`search_corpus` 对外 `strategy` enum 移除 `legacy`，只暴露 `auto|bm25|vector|hybrid`；旧 plan payload 若仍传 `legacy` 会在 registry helper 中归一为 `auto`，避免模型继续学习历史策略名。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 595 个测试。
- 2026-04-30 继续按 review §8/M2 清理 router target recovery legacy 命名：`fallback_query_targets` 改为 `query_target_candidates`，`fallback_target_aliases` 改为 `loss_notation_target_aliases`，让 LLM router 兼容补目标逻辑不再挂在旧 fallback 语义上。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 595 个测试。
- 2026-04-30 继续按 review M1/M2 清理 runtime summary legacy alias 命名：`LEGACY_TOOL_NAME_ALIASES` 改为 `INTERNAL_TOOL_STAGE_ALIASES`，runtime summary 的 `legacy_tools` 字段改为 `noncanonical_tools`，保留内部 stage 到 canonical tool 的映射但不再作为 legacy tool surface 暴露。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 595 个测试。
- 2026-04-30 继续按 review §6/M3 落地动态工具 sandbox-test runner：`run_tool_proposal_sandbox` 仅允许 `approved_for_sandbox_test/approved_for_runtime` 提案在隔离 Python 子进程中执行，执行前校验 code hash、静态 AST 与输入 schema，执行时限制 builtins/import、超时、内存、文件大小和输出长度，并生成可用于 runtime approval 的结构化 sandbox report；`approved_for_runtime` 现在要求 report 状态为 `pass`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 598 个测试。
- 2026-04-30 继续按 review §6/M3 推进动态工具 runtime registration 前置层：新增 `runtime_tool_manifest/load_runtime_tool_manifests`，仅把 `approved_for_runtime`、sandbox report 为 `pass`、code hash 未篡改、schema 有效且不与静态工具名冲突的提案转成动态工具 metadata，并且不暴露 `python_code`、不自动接入 planner/registry。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 601 个测试。
- 2026-04-30 继续按 review §8.2/M2 收敛 `QueryContract.notes` magic string 解析：planner intent payload、answer composer、claim verifier、solver goal、research planning 与 retrieval 不再各自手写 `answer_slot=`/intent/ambiguity/confidence/target_alias 解析，统一走 `contract_context` helper，为后续把 QueryContract 从控制流驱动降为只读标签继续铺路。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 601 个测试。
- 2026-04-30 继续按 review §8.2/M2 收敛 `QueryContract.notes` 解析副本：trace diff、confidence scoring 与 conversation clarification report 也改用 `contract_context.note_value/note_values/note_float`，删除局部 `_note_*` helper 与手写 ambiguous_slot 解析。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 601 个测试。
- 2026-04-30 继续按 review §8.2/M2 收敛 `QueryContract.notes` 布尔控制流读取：新增 `contract_has_note/has_note`，将 `exclude_previous_focus`、`needs_contextual_refine`、`citation_count_requires_web`、`auto_resolved_by_llm_judge` 等精确 flag 判断从 Agent/runtime/planner/clarification/composer/registry 中收回统一 helper。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 601 个测试。
- 2026-04-30 继续按 review §6/M3 加固动态工具 sandbox-test runner：子进程现在在空临时工作目录中执行，不再继承项目 cwd；sandbox report 记录 `working_directory=temporary_empty` 与进程数限制，配合已有 builtins/import、超时、内存、文件大小和输出截断限制。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 601 个测试。
- 2026-04-30 继续按 review §6/M3 接入默认关闭的动态工具 runtime：`AgentSettings/Settings` 新增 `agent_dynamic_tools_enabled`、动态工具超时和内存限制；开启后 runtime manifest 会暴露给 planner、进入 runtime action filtering，并在 conversation/research registry 中注册为 sandbox 执行工具，每次调用仍通过 `run_tool_proposal_sandbox`，默认关闭时不改变现有工具集。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 603 个测试。
- 2026-04-30 继续按 review §6/M3 加固动态工具反射逃逸面：`proposed_tools.py` 将 `getattr/hasattr/setattr/delattr` 纳入静态 blocked call，并从 sandbox safe builtins 移除，避免 `getattr(obj, "__class__")` 绕过 dunder attribute 限制。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 604 个测试。
- 2026-04-30 继续按 review §4/§9 为删除专用 solver 铺路：新增 `solver_dispatch.deterministic_solver_stages` 纯函数，把 `solver_pipeline.py` 中按 goals 分派 15+ 专用 solver 的顺序抽成可测试 stage list，`_solve_claims_with_deterministic_fallback` 改为消费该 stage list 后再调用现有 solver；行为保持兼容但分派边界可观测。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 607 个测试。
- 2026-04-30 继续按 review §10/M3 为 solver replacement 增加 trace 护栏：`agent_trace_diff.py` 将 research plan 的 `solver_sequence` 与 `required_claims` 纳入稳定签名，后续替换专用 solver/composer 时可以直接发现计划路径漂移。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 608 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent 事件转发壳：新增 `agent_tool_events.py` 承接 tool_call/observation canonical emission，Agent、Task、tool registry 与 helper 直接调用该边界，并删除 `ResearchAssistantAgentV4._emit_agent_tool_call/_record_agent_observation`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 608 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent 资料查询转发壳：新增 `paper_summary_helpers.paper_summary_text`，Agent、concept/entity mixin、follow-up ranking 与 solver claim assembly 直接使用该 helper，并删除 `ResearchAssistantAgentV4._paper_summary_text`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 611 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent PDF 渲染转发壳：table/figure VLM 的 page image rendering 改为在 `solver_pipeline.py` 直接调用 `pdf_rendering.render_pdf_page_image_data_url`，保留 Agent render cache 但删除 `ResearchAssistantAgentV4._render_page_image_data_url`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 611 个测试。
- 2026-04-30 继续按 review §8/M2 清理 router/adapter 转发壳：`router_miss_clarification_contract` 下沉到 `intent_router.py`，`_extract_query_contract` 直接调用 LLM router、typed contract adapter 与 conversation tool normalizer，并删除 `_router_miss_clarification_contract/_contract_from_llm_tool_router/_normalize_conversation_tool_contract` 三个 Agent 包装方法。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 612 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent memory 转发壳：research turn 与 compound turn 直接调用 `research_memory.remember_research_outcome/remember_compound_outcome` 并注入 `candidate_lookup`，删除 `ResearchAssistantAgentV4._remember_research_outcome/_remember_compound_outcome`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 612 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent conversation answer 转发壳：新增 `conversation_answer_state.set_conversation_answer`，conversation/research tool registry 直接写入 answer state 并 emit `answer_delta`，删除 `ResearchAssistantAgentV4._set_conversation_answer`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 612 个测试。
- 2026-04-30 继续按 review §6/M3 清理动态工具初始化边界：新增 `dynamic_tool_context.load_agent_dynamic_tool_manifests`，把默认关闭与 invalid runtime manifest fail-closed 逻辑移出 Agent，并删除 `ResearchAssistantAgentV4._load_dynamic_tool_manifests`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 614 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent session history 转发壳：`session_context_helpers.compress_session_history_if_needed` 统一处理压缩窗口、prompt/payload、LLM 摘要、apply 与 session 持久化，删除 `ResearchAssistantAgentV4._compress_session_history_if_needed`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 615 个测试。
- 2026-04-30 继续按 review §8/M2 收敛上下文 contract 修正规则：新增 `contextual_contract_resolver.resolve_contextual_research_contract`，将公式纠错、公式位置追问、当前论文绑定与论文范围纠错从 Agent 五个方法中移出，并补充 resolver 单测覆盖 active-paper 和 formula-paper binding。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 617 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent disambiguation 转发壳：证据消歧路径直接调用 `clarification_intents.acronym_options_from_evidence/acronym_evidence_from_corpus/disambiguation_judge_option_payload`，删除 Agent 内三个局部包装方法。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 617 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent paper hint 转发壳：`_extract_query_contract` 内部以局部 `paper_hint_lookup` 注入 conversation/contextual contract adapter，直接调用 `contextual_contract_helpers.paper_from_query_hint`，删除 `ResearchAssistantAgentV4._paper_from_query_hint`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 617 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent conversation context 转发壳：新增 `session_context_helpers.agent_session_conversation_context` 统一封装 persistent learnings 注入与会话上下文组装，planner/router/compound/tool registry/mixins 直接调用该 helper，删除 `ResearchAssistantAgentV4._session_conversation_context`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent clarification question 转发壳：新增 `clarification_question_helpers.build_agent_clarification_question` 承接 clients/settings/context 注入，agent loop、compound、tool registry 与 answer composer 直接调用 helper，删除 `ResearchAssistantAgentV4._clarification_question`。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent follow-up candidate 转发壳：`solver_pipeline.py` 直接调用 `followup_candidate_helpers.resolve_followup_seed_papers/expand_followup_candidate_pool/rank_followup_candidates` 并注入 retriever/summary/confidence 依赖，删除 Agent 内三个 follow-up candidate 包装方法，`agent.py` 降至 1135 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent reflection 转发壳：`_agent_reflect` 内部直接组装 focus title lookup、target binding 与 ambiguity option count，并调用 `agent_runtime_helpers.reflect_agent_state_decision`，删除 `ResearchAssistantAgentV4._reflect_agent_state`，`agent.py` 降至 1119 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent disambiguation runtime 边界：新增 `agent_disambiguation_runtime.py` 承接证据消歧 option 构造、LLM judge payload 解析和选中候选后的材料刷新，删除 `ResearchAssistantAgentV4._disambiguation_options_from_evidence/_judge_disambiguation_options/_refresh_state_for_selected_ambiguity`，`agent.py` 降至 1010 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent clarification-limit 转发壳：新增 `clarification_limit_runtime.force_best_effort_after_clarification_limit` 统一执行澄清次数上限后的 best-effort retry 与事件记录，`agent_loop.py` 直接调用该 runtime helper，删除 `ResearchAssistantAgentV4._force_best_effort_after_clarification_limit`，`agent.py` 降至 955 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent compound Task 转发壳：`agent_compound.execute_compound_task_subagent` 接管 compound subtask 的 Task 调用、结果归一和 observation 记录，compound 拆解流程直接调用该 helper，删除 `ResearchAssistantAgentV4._execute_compound_task_subagent`，`agent.py` 降至 913 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent research search handler 边界：新增 `agent_research_search_handlers.py` 承接 paper search、screening、corpus evidence search 与 web evidence merge，research tool registry 直接调用这些 handler，删除 `ResearchAssistantAgentV4._agent_search_papers/_screen_agent_papers/_agent_search_evidence/_agent_web_search`，`agent.py` 降至 739 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent research verification handler 边界：新增 `agent_research_verification_handlers.py` 承接 verify grounding 与 retry research 编排，research tool registry 直接调用 `agent_verify_grounding`，删除 `ResearchAssistantAgentV4._agent_verify_grounding/_agent_retry_after_verification`，`agent.py` 降至 581 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent research compose handler 边界：新增 `agent_research_compose_handlers.py` 承接 compose/solve_claims、证据消歧判断与自动解析后的 claim 生成，research tool registry 直接调用 `agent_solve_claims`，删除 `ResearchAssistantAgentV4._agent_solve_claims`，`agent.py` 降至 435 行。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent research reflection handler 边界：新增 `agent_research_reflection_handlers.py` 承接 verification 后的 reflection 决策、focus title 查找和澄清判定，`finalize_research_runtime` 直接调用 `agent_reflect`，删除 `ResearchAssistantAgentV4._agent_reflect`，`agent.py` 降至 371 行。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent chat runtime 边界：新增 `agent_chat_runtime.py` 承接 session 压缩、run context、compound/standard turn 分派与 final 收尾，`chat/astream_chat_events` 直接调用 `run_agent_chat_turn`，删除 `ResearchAssistantAgentV4._run`，`agent.py` 降至 312 行。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review M1/M2 清理 Agent query contract extraction 边界：新增 `agent_contract_extraction.py` 承接澄清选择、LLM router adapter、conversation tool normalization、上下文 research contract refine、follow-up relationship/direction 继承与会话记忆绑定，standard turn 与 Task subagent 直接调用 `extract_agent_query_contract`，删除 `ResearchAssistantAgentV4._extract_query_contract`，`agent.py` 降至 196 行。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 618 个测试。
- 2026-04-30 继续按 review §10/M3 增强 solver/composer 替换前的 trace 护栏：`agent_trace_diff.py` 现在对 `claims` 事件生成稳定 claim signature，覆盖 claim type/entity/value preview、paper/evidence ids 与 structured source，能在替换专用 solver 前发现 claim 层漂移。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 619 个测试。
- 2026-04-30 继续按 review M1/M2 清理 legacy 命名表面：runtime summary 中无 explicit source 的 claim 现在归为 `deterministic_solver` 而不是 `legacy_solver`，composer 的 deterministic claim 兼容开关也改名为 `include_deterministic`，减少当前主路径被标为 legacy 的歧义。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 620 个测试。
- 2026-04-30 继续按 review §10/M3 增强 solver/composer 替换前的 trace 护栏：`agent_trace_diff.py` 现在对 `evidence`/`web_search` 事件生成稳定 evidence signature，覆盖 doc/paper id、标题、页码、block type、snippet preview、source 与路径，能在替换专用 solver/compose 前发现检索证据漂移。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 621 个测试。
- 2026-04-30 继续按 review §6/§11 硬化动态工具生产边界：`propose_tool` 写入 deployment/session scope，runtime manifest 暴露同一 scope，并由 `agent_dynamic_tool_deployment_id` 过滤加载，避免已批准动态工具跨部署暴露；默认动态工具仍关闭。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 623 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract 降级：active research 会话记忆更新现在通过 `session_context_helpers.active_research_from_contract` / `conversation_active_research_from_contract` 统一映射，`agent_loop.py` 不再直接拼装这些 contract 字段，后续可继续把 contract 当只读上下文标签收口。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 624 个测试。
- 2026-04-30 继续按 review §10/M3 增强 composer 替换前的 trace 护栏：`agent_trace_diff.py` 现在对 final payload 生成 citation signature，覆盖 citation doc/paper id、标题、页码、block type、snippet preview 与路径，避免后续 generic compose 改造时引用漂移只靠答案长度发现。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 625 个测试。
- 2026-04-30 继续按 review §6/M3 为删除 15+ 专用 solver 搭桥：新增默认关闭的 `agent_generic_claim_solver_enabled`，开启后 `_run_solvers` 会先尝试 schema-based generic claim solver，再回落 deterministic specialized solvers；默认行为不变，但 eval/灰度可开始验证通用 solver 替换路径。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 626 个测试。
- 2026-04-30 继续按 review §8 降低 QueryContract notes magic-string 分散度：新增 `contract_context.note_json_value(s)`，`selected_ambiguity_option=` JSON note 解析由 answer composer 与 clarification helpers 共享同一入口，`ambiguous_slot=` 读取也改用 `note_values`；行为不变。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 626 个测试。
- 2026-04-30 继续按 review §8 降低 QueryContract notes magic-string 分散度：新增 `contract_context.notes_without_prefixes`，auto-resolved ambiguity 与 refreshed ambiguity options 清理旧 `ambiguity_option=` / selected / judge notes 时使用共享 prefix adapter，避免各处手写 startswith 过滤。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 626 个测试。
- 2026-04-30 继续按 review §6/M3 为删除 15+ 专用 solver 增加影子对照：新增默认关闭的 `agent_generic_claim_solver_shadow_enabled`，开启后 schema-based generic claim solver 与 deterministic specialized solver 并行运行并输出 `solver_shadow` trace/observation，默认回答路径不变，但灰度/eval 可比较 claim count、type、paper/evidence ids 与 source。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 628 个测试。
- 2026-04-30 继续按 review §10/M3 增强 solver 替换前 trace 护栏：`agent_trace_diff.py` 现在对 `solver_shadow` 事件生成稳定签名，覆盖 selected solver path 以及 schema/deterministic 两侧的 claim count、type、paper/evidence ids 与 source counts，灰度替换 generic solver 时可直接比较影子漂移。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §4/§10 收敛旧内部工具别名表面：research event payload 的 `stage` 不再输出 `search_papers/search_evidence/solve_claims/verify_grounding`，改为 `paper_discovery/evidence_retrieval/claim_composition/grounding_verification` 这类阶段标签，保留 canonical public tools 但减少旧别名继续进入 trace/frontend/eval。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §4/§10 收敛内部 stage alias 表：verification retry 不再以 `retry_research` 工具名发事件，而是直接使用 canonical `search_corpus` 并携带 `stage=research_retry`；同步删除 `retry_research -> search_corpus` alias，减少 runtime summary/trace 对旧别名映射的依赖。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §4/§10 收敛内部 stage alias 表：证据消歧不再以 `resolve_ambiguity/detect_ambiguity` 作为 observation tool，而是直接发 canonical `compose/ask_human` 并在 payload 中标记 `ambiguity_resolution/ambiguity_detection` stage；同步删除两条 alias 映射。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §4/§10 收敛 conversation compose 别名：普通对话、库状态、库推荐不再以 `answer_conversation/get_library_status/get_library_recommendation` 作为事件 tool，而是直接发 canonical `compose` 并携带 `conversation_answer/library_status/library_recommendation` stage；工作记忆 artifact 类型保持不变以维持追问上下文。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §4/§10 收敛 read_memory 别名：intent understanding、conversation memory read、memory follow-up answer、memory synthesis、previous-answer reflection 与 recommendation recovery 不再作为 observation/tool_call 的 tool 名出现，统一发 canonical `read_memory` 并用 stage 标记内部阶段；工作记忆 artifact 类型保持不变。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §4/§10 收敛 citation ranking 别名：引用数 lookup 不再发 `web_citation_lookup`，统一使用 canonical `web_search` + `stage=citation_count_lookup`；引用数排序成文不再发 `rank_by_verified_citation_count`，统一使用 canonical `compose` + `stage=citation_count_ranking`；同步删除两条 alias 映射。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §4/§10 清空内部 stage alias 表：删除未注册的 `compose_or_ask_human` 死函数/文案，clarification-limit 事件直接发 canonical `ask_human` + `stage=clarification_limit`，`INTERNAL_TOOL_STAGE_ALIASES` 现在为空；runtime summary 仍会显式报告未知非 canonical tool。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §6/M3 硬化动态工具 runtime approval：`approved_for_runtime` 现在要求 sandbox report 的 `proposal_id/tool_name/code_sha256` 与当前 proposal 完全一致，runtime manifest 加载时也重复校验，防止用其他工具或旧代码的 pass report 批准当前动态工具。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §6/M3 硬化动态工具 sandbox policy：runtime approval 现在要求 sandbox report 声明 subprocess、isolated Python、temporary empty cwd，并且 network/filesystem policy 均为 deny 模式；伪造 `network=allow` 的 pass report 会被拒绝。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §4/§10 删除空兼容表：移除已清空的 `INTERNAL_TOOL_STAGE_ALIASES` 常量，runtime event canonicalization 与 runtime summary 直接使用显式空 alias map，避免继续保留命名上的内部 stage alias 入口。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §6/M3 硬化动态工具沙箱报告契约：sandbox report 现在带 `tool_sandbox.v1` policy version，runtime approval 与 runtime manifest 加载都会拒绝旧版本/伪造版本的通过报告，避免未来沙箱策略升级后复用 stale pass report。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 629 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract 降级：research planning 新增 typed `ResearchPlanContext` 与 `build_research_plan_from_context` 入口，`build_research_plan` 仅作为 QueryContract 兼容适配层，plan limits/solver sequence/required claims 后续可直接由显式规划标签驱动。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 631 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract 降级：`query_shaping` 的 paper/evidence query text 与 concept evidence routing 新增 `ResearchPlanContext` 入口，旧 QueryContract API 只做兼容适配，检索 shaping 不再直接重新读取 contract 推断 goals。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 632 个测试。
- 2026-04-30 继续按 review §4/§10 为删除专用 solver 铺路：`solver_dispatch.py` 新增 typed `SolverDispatchContext` 与 context-based deterministic dispatch 入口，`solver_pipeline.py` 先组装 dispatch context 再选择专用 solver stage，旧 goals/modalities API 仅保留为兼容 wrapper。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 633 个测试。
- 2026-04-30 继续按 review §4/§10 为删除专用 solver 铺路：`solver_goal_helpers.py` 新增 typed `ClaimGoalContext`，deterministic solver pipeline 与 schema solver eligibility 均先从 context 推断 goals，旧 `claim_goals(contract, plan)` 仅保留为兼容 wrapper。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 635 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract 降级：`web_evidence` 的 web query、domain allowlist 与 web-claim append decision 新增 `ResearchPlanContext` 入口，旧 QueryContract API 仅做兼容适配，外部证据路径不再直接从 contract 重新推断 goals。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 636 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract 降级：conversation memory target binding、contextual formula/paper repair 与 follow-up relationship inheritance 改为通过 `ResearchPlanContext` 读取 goals，不再直接调用 QueryContract goal inference helper。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 636 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract 降级：clarification question fallback 与 evidence disambiguation routing 改为通过 `ResearchPlanContext` 读取 goals，澄清路径不再直接调用 QueryContract goal inference helper。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 636 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract 降级：follow-up refinement 与 runtime material screening/retry helpers 改为通过 `ResearchPlanContext` 读取 goals；除 `research_planning` 自身兼容 adapter 外，服务层不再直接调用 `research_plan_goals(contract)`。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 636 个测试。
- 2026-04-30 继续按 review §6/M3 硬化动态工具 runtime approval：sandbox report 现在必须包含 64 位 `input_sha256` 和受限资源 limits，runtime manifest 也暴露 input hash 审计字段；伪造无输入哈希或超时过大的 pass report 会被拒绝。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 636 个测试。
- 2026-04-30 继续按 review §8 增加 QueryContract 降级回归护栏：新增架构边界测试，禁止服务模块在 `research_planning` 兼容 adapter 外重新引入 `research_plan_goals(contract)` 直接调用。使用 `zotero-paper-rag` conda 环境编译检查、相关测试与全量 `pytest` 通过 637 个测试。
- 2026-04-30 前端切换到 V5 工作台：根路由改为 `/v5`，保留 `/v4` 旧入口；静态 UI 重命名为 Paper Agent V5，主工作区新增 workflow-first 的 Intent/Evidence/Status 运行状态条，右侧证据面板改为 Evidence Board，图标按钮和本地历史 key 切到 v5；新增前端路由回归测试。使用 `zotero-paper-rag` conda 环境专项 `tests/test_frontend_v5.py` 与全量 `pytest -q` 通过。
- 2026-04-30 继续按 review §4/§10 为删除专用 solver 铺路：新增 `generic_claim_solver.py`，把 schema-based generic claim solver 的 prompt/payload/claim 转换从 `solver_pipeline.py` 中抽出；pipeline 只负责提供会话上下文和调度，helper 通过统一 `ModelClients.invoke_json` adapter 调用模型，并在无 chat/无证据时兼容返回空结果。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_generic_claim_solver.py tests/test_solver_pipeline.py tests/test_agent_v4.py` 与全量 `pytest -q` 通过。
- 2026-04-30 V5 前端继续对齐 review SSE 协议：Run 面板新增 TODO 区，流式处理优先使用标准化 `data.type`，可展示 `todo_update / tool_use / tool_result / confidence / ask_human` 事件，兼容旧 `tool_call / observation` 事件名。使用 `zotero-paper-rag` conda 环境专项 `tests/test_frontend_v5.py` 与全量 `pytest -q` 通过。
- 2026-04-30 继续按 review §4/§10 去除 solver 假耦合：`_run_solvers`、claim compose handler 和 grounding verification/retry handler 不再接收未使用的 `use_web_search / max_web_results` 参数，web 检索决策留在 material/search 阶段，claim solving 只消费 contract/plan/papers/evidence/session。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_solver_pipeline.py tests/test_agent_tool_registry_and_active_research.py tests/test_agent_v4.py tests/test_agent_loop.py` 与全量 `pytest -q` 通过。
- 2026-04-30 修复 V5 交互中暴露的 paper-source 纠错路由问题：`ORIGIN_LOOKUP_MARKERS` 增加“是哪篇论文/论文来源/source paper”等来源查询表达，LLM router 在低置信或 `need_clarify` 时如果检测到明确目标和论文来源意图，会恢复为 `origin_lookup` research contract，并以 `context_switch` 脱离上一轮错误 active paper；新增 DeepSeek/CommunityBench 误绑定回归测试。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_research_intents.py tests/test_intent_router.py tests/test_agent_v4.py` 与全量 `pytest -q` 通过 643 个测试。
- 2026-04-30 继续按 review §6/M3 硬化动态工具 runtime approval：runtime manifest 除了校验 sandbox report 之外，现在还必须存在有效的 `approved_for_sandbox_test -> approved_for_runtime` 审批日志，且 reviewer 与 code hash 可审计；manifest 暴露 `runtime_approval` 摘要，伪造无审批链的 runtime payload 会被拒绝。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_proposed_tools.py tests/test_dynamic_tool_context.py tests/test_agent_tool_registry_and_active_research.py` 与全量 `pytest -q` 通过 644 个测试。
- 2026-04-30 继续按 review §6/M3 硬化动态工具执行边界：`run_tool_proposal_sandbox` 对 `approved_for_runtime` 状态的工具执行前也重新校验 sandbox report 与 runtime approval review log，防止服务启动后 proposal 文件被手工篡改成 runtime 状态绕过审批链。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_proposed_tools.py tests/test_dynamic_tool_context.py tests/test_agent_tool_registry_and_active_research.py` 与全量 `pytest -q` 通过 645 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract notes 降级：follow-up relationship 的 `candidate_title=` 读取与清理改用 `contract_context.note_value/notes_without_prefixes`，减少跟进关系路径里的手写 magic-string startswith 解析。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_followup_relationship_contracts.py tests/test_followup_candidate_helpers.py tests/test_contract_context.py tests/test_agent_v4.py` 与全量 `pytest -q` 通过 645 个测试。
- 2026-04-30 继续按 review §8 推进 QueryContract 控制流降级：answer composer 的 web research answer gating 不再维护本地 requested-field/answer-slot goal 映射表，改为复用 `research_plan_context_from_contract(contract).goals`，让 composer 与 planner 使用同一 typed goal 入口。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_web_evidence.py tests/test_agent_v4.py tests/test_agent_tool_registry_and_active_research.py tests/test_research_planning.py` 与全量 `pytest -q` 通过 645 个测试。
- 2026-04-30 继续按 review M1/M2 清理 answer composer 兼容壳：删除 `AnswerComposerMixin._answer_slots` 私有转发方法，composer 直接调用 `contract_context.contract_answer_slots`，进一步减少 mixin 内 QueryContract helper 包装层。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_agent_v4.py tests/test_contract_context.py` 与全量 `pytest -q` 通过 645 个测试。
- 2026-04-30 继续按 review §10/M3 为替换专用 solver 铺路：`generic_claim_solver.py` 接管 schema-vs-deterministic shadow summary 和 claim summary 构造，`solver_pipeline.py` 只负责触发 shadow 对照，不再拥有 trace payload 形状；新增稳定 summary 单测。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_generic_claim_solver.py tests/test_solver_pipeline.py tests/test_agent_tool_registry_and_active_research.py` 与全量 `pytest -q` 通过 646 个测试。
- 2026-04-30 继续按 review §10/M3 清理 solver pipeline 私有转发壳：删除 `SolverPipelineMixin._should_use_schema_claim_solver`，pipeline 直接调用 `schema_claim_helpers.should_use_schema_claim_solver`，测试也直接覆盖 helper，减少 mixin API 表面。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_solver_pipeline.py tests/test_schema_claim_helpers.py tests/test_agent_v4.py` 与全量 `pytest -q` 通过 646 个测试。
- 2026-04-30 修复 V5 实测暴露的 figure 查询论文身份误绑定：`screen_agent_papers` 对 explicit figure target 不再 fallback 到所有候选，必须保留标题/alias identity match，避免 CommunityBench 这类正文提到 DeepSeek 的论文被当成 DeepSeek 论文；formula 查询仍保留无 identity match 时的候选 fallback，避免 PBA 等方法名误伤。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_agent_runtime_helpers.py tests/test_agent_v4.py tests/test_agent_tool_registry_and_active_research.py` 与全量 `pytest -q` 通过 648 个测试。
- 2026-04-30 继续按 review §6/M3 硬化动态工具 OS 隔离审计：sandbox report 的 `sandbox.resource_limits` 现在必须声明地址空间、CPU、文件大小、open files 和进程数限制，并与 runtime limits 一致；runtime approval 与 manifest 加载会拒绝缺失或超限的资源审计，真实 sandbox report 也暴露这些 OS 级限制。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_proposed_tools.py tests/test_dynamic_tool_context.py tests/test_agent_tool_registry_and_active_research.py` 与全量 `pytest -q` 通过 648 个测试。
- 2026-04-30 继续按 review §7 推进答案置信度：新增默认关闭的 `agent_answer_self_consistency_enabled` / `agent_answer_self_consistency_samples`，开启后研究回答会对 final answer 额外采样并基于 `confidence_from_self_consistency` 发出 `confidence` 事件、写入 runtime summary；与已存在的 logprobs confidence 并行，默认线上延迟不变。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_agent_loop.py tests/test_confidence.py tests/test_agent_tool_registry_and_active_research.py` 与全量 `pytest -q` 通过 649 个测试。
- 2026-04-30 继续按 review §8 降低 `QueryContract.notes` 隐式总线暴露：`contract_context` 新增 `contract_notes/contract_note_value(s)/contract_note_json_value/contract_notes_without_prefixes`，并迁移答案 composer、澄清消歧、工具摘要、follow-up candidate title 等路径，减少各模块直接读取 `contract.notes` 与手写 prefix 解析。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_contract_context.py tests/test_clarification_intents.py tests/test_agent_v4.py tests/test_tool_registry_helpers.py tests/test_followup_candidate_helpers.py` 与全量 `pytest -q` 通过 649 个测试。
- 2026-04-30 继续按 review §8 推进 planner/runtime summary 的 QueryContract 降级：新增 `contract_note_float`，`agent_runtime_summary.py` 与 `agent_planner_helpers.py` 改用 contract-shaped note adapter 读取 intent kind/confidence、ambiguous slots、target aliases 和 notes payload，避免汇总/规划层继续手写 notes 解析。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_contract_context.py tests/test_agent_runtime_summary.py tests/test_agent_planner_helpers.py tests/test_agent_v4.py` 与全量 `pytest -q` 通过 649 个测试。
- 2026-04-30 继续按 review §8 收口 raw `contract.notes` 访问面：follow-up routing、conversation memory binding、clarification limit/best-effort、reflection next-action、retrieval target alias、schema claim prompt、contextual/conversation contract helpers 与 compound task 合并均改用 `contract_notes`/contract note adapters；服务层直接读取 raw notes 的位置收敛到 `contract_context` 和 `research_planning` 兼容 adapter。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_contract_context.py tests/test_confidence.py tests/test_conversation_memory_contract.py tests/test_conversation_contract_helpers.py tests/test_contextual_contract_helpers.py tests/test_agent_runtime_helpers.py tests/test_compound_task_helpers.py tests/test_indexing_and_retrieval_hardening.py tests/test_agent_v4.py` 与全量 `pytest -q` 通过 649 个测试。
- 2026-04-30 继续按 review §10/M3 拆薄专用 solver pipeline：新增 `deterministic_solver_runner.py`，把 stage 名称到专用 solver 方法的分发表从 `solver_pipeline.py` 抽出，`solver_pipeline.py` 降至 820 行；新增 table_metric 分支回归测试，确保 metric context 补充逻辑在抽出后保持一致。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_deterministic_solver_runner.py tests/test_solver_pipeline.py tests/test_solver_dispatch.py tests/test_generic_claim_solver.py tests/test_agent_v4.py` 与全量 `pytest -q` 通过 652 个测试。
- 2026-04-30 继续按 review §6/M3 推进动态工具生产审核面：新增 admin-only `/api/v1/v4/admin/tools/proposals` API，可列出/查看 proposal、运行 sandbox test、执行 status transition；接口受 `ADMIN_API_KEY` 保护，列表默认不暴露 `python_code`，仍不改变默认关闭的 runtime 注册策略。`docs/tool-sandbox.md` 同步记录 Admin API。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_dynamic_tool_admin_api.py tests/test_proposed_tools.py tests/test_api_routes.py tests/test_security.py` 与全量 `pytest -q` 通过 654 个测试。
- 2026-04-30 继续按 review §8 增加 QueryContract notes 降级护栏：架构边界测试现在禁止服务层在 `contract_context.py` 与 `research_planning.py` 兼容 adapter 外直接读取 `contract.notes`，防止后续重构重新扩散 magic-string bus。使用 `zotero-paper-rag` conda 环境专项 `tests/test_review_architecture_boundaries.py` 与全量 `pytest -q` 通过 655 个测试。
- 2026-04-30 继续按 review §10/M3 增强 solver/composer 替换护栏：`agent_trace_diff.py` 的 final 事件签名现在纳入 `runtime_summary` 中的 grounding status、claim/citation count、claim source 和 answer confidence basis/score bucket，后续切换 generic compose/solver 时能发现运行时质量信号漂移。使用 `zotero-paper-rag` conda 环境专项 `tests/test_agent_trace.py` 与全量 `pytest -q` 通过 656 个测试。
- 2026-04-30 继续按 review §10/M3 清理 solver pipeline 私有兼容壳：删除仅测试使用的 `SolverPipelineMixin._solve_text`，相关回归测试直接调用 `_solve_claims_with_deterministic_fallback` 真实入口；`solver_pipeline.py` 降至 803 行。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_agent_v4.py tests/test_solver_pipeline.py tests/test_deterministic_solver_runner.py` 与全量 `pytest -q` 通过 656 个测试。
- 2026-04-30 继续按 review §7 增强置信度链路护栏：为 `ModelClients.stream_text` 增加 logprobs streaming 回归测试，确保开启 provider logprobs 时每个 streamed chunk 只发出一次 logprob batch，避免未来启用 provider confidence 后重复计数。使用 `zotero-paper-rag` conda 环境相关测试 `tests/test_model_clients_tool_plan.py tests/test_agent_loop.py tests/test_confidence.py` 与全量 `pytest -q` 通过 657 个测试。
- 2026-04-30 继续按 review §10/M3 拆薄专用 solver pipeline：将来源论文选择、corpus intro 候选补充和 intro-support 排序从 `solver_pipeline.py` 下沉到 `origin_selection_helpers.py`，`solver_pipeline.py` 从 803 行降至 697 行；DeepSeek/AlignX 来源论文路径相关回归测试与全量 `pytest -q` 通过 657 个测试。
- 2026-04-30 继续按 review §10/M3 清理 solver pipeline 私有转发壳：概念解释与 follow-up candidate 置信度统一走 `confidence.coerce_claim_confidence`，metric line 抽取和公式 block 评分直接调用各自 helper，并删除 formula normalize 等测试用私有包装；`solver_pipeline.py` 降至 669 行。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest -q` 通过 658 个测试。
- 2026-04-30 继续按 review §10/M3 抽出公式专用 solver：新增 `formula_claim_solver.py` 承接公式证据筛选、LLM 公式抽取和 fallback claim 生成，`solver_pipeline.py` 仅保留调度入口并降至 607 行；新增无模型 fallback 公式 claim 单测。使用 `zotero-paper-rag` conda 环境相关测试与全量 `pytest -q` 通过 659 个测试。
- 2026-04-30 继续按 review §10/M3 抽出表格 metric solver：新增 `table_claim_solver.py` 承接 table evidence ranking、VLM page image 渲染调用和 text-table fallback claim 生成，`solver_pipeline.py` 降至 541 行；现有 table/VLM/primary-paper 回归测试迁到新模块边界并通过，全量 `pytest -q` 通过 659 个测试。
- 2026-04-30 继续按 review §10/M3 抽出 figure solver：新增 `figure_claim_solver.py` 承接 figure context 选择、benchmark fallback、VLM 图像理解与文本总结 fallback，删除 answer composer 中只服务 figure solver 的跨 mixin 私有方法；`solver_pipeline.py` 降至 478 行。使用 `zotero-paper-rag` conda 环境 figure/visual 相关测试与全量 `pytest -q` 通过 659 个测试。
- 2026-04-30 继续按 review §10/M3 抽出 follow-up research solver：新增 `followup_research_solver.py` 承接 seed paper 解析、候选扩展、候选评分和 follow-up claim 生成，`solver_pipeline.py` 降至 411 行；follow-up candidate/relationship/compound 相关测试与全量 `pytest -q` 通过 659 个测试。
- 2026-04-30 继续按 review §10/M3 抽出文本类 deterministic solver：新增 `text_claim_solver.py` 承接 topology discovery/recommendation、paper summary、paper recommendation、metric context 和 default text claim 生成，`solver_pipeline.py` 降至 355 行；summary/metric/topology/recommendation 相关测试与全量 `pytest -q` 通过 659 个测试。
- 2026-04-30 继续按 review §10/M3 抽出 origin lookup claim solver：新增 `origin_claim_solver.py` 承接来源论文候选选择与 origin claim 生成，`solver_pipeline.py` 降至 347 行；DeepSeek/AlignX 来源论文相关测试与全量 `pytest -q` 通过 659 个测试。
- 2026-04-30 继续按 review §10/M3 收敛 deterministic solver runner：`deterministic_solver_runner.py` 的 stage 分发从 if 链改为显式 stage→method registry，table metric 的 metric-context 补充分支保留为独立 helper；runner/solver 相关测试与全量 `pytest -q` 通过 659 个测试。
- 2026-04-30 继续按 review §6/M3 补齐 V5 动态工具审核前端：右侧 inspector 新增 Tools/Tool Review 面板，可输入 `ADMIN_API_KEY`、列出/查看 proposal、批准沙箱、运行 sandbox、批准 runtime 或撤销 proposal，并用本地 key `zotero-agent-v5-admin-key` 记住管理员 key；前端回归测试覆盖 admin proposal 入口，动态工具 admin/security 相关测试与全量 `pytest -q` 通过 659 个测试。
- 2026-04-30 继续按 review §10/M3 增强 tool-use trace 护栏：`agent_trace_diff.py` 现在把 `tool_call/tool_use` 的结构化 input 纳入稳定签名，能发现同一工具名下 query/top_k/filter 等参数漂移；trace/event 相关测试与全量 `pytest -q` 通过 660 个测试。
- 2026-04-30 继续按 review §10/M3 增强 tool-use step trace 护栏：`agent_trace_diff.py` 现在也记录 `agent_step` 的 action 与 arguments 稳定签名，能在执行步层面发现结构化参数变化；trace/event 相关测试与全量 `pytest -q` 通过 661 个测试。
- 2026-04-30 继续按 review §10/M3 拆薄 solver pipeline orchestration：新增 `claim_solver_pipeline.py` 承接 schema generic solver、deterministic fallback 与 shadow parity 的选择逻辑，`SolverPipelineMixin` 只负责注入依赖并接收结果；新增 pipeline 单测，solver 相关测试与全量 `pytest -q` 通过 663 个测试。
- 2026-04-30 继续按 review §10/M3 拆薄 deterministic solver fallback：新增 `deterministic_claim_solver.py` 承接 goal context、stage dispatch、claim 去重和 default fallback 调度，`SolverPipelineMixin` 只作为 runner 依赖注入；新增 deterministic claim solver 单测，solver/agent 相关测试与全量 `pytest -q` 通过 665 个测试。
- 2026-04-30 继续按 review §4/M2 推进结构化 tool-use executor：`RegisteredAgentTool` 支持 argument-aware handler，`execute_tool_loop` 将当前 step 的结构化 arguments 传入 executor，dependency 可通过 argument provider 读取自己的 planned input；动态工具优先消费本次 step arguments，旧无参 handler 保持兼容。工具/runtime/动态工具/agent 相关测试与全量 `pytest -q` 通过 667 个测试。
- 2026-04-30 继续按 review §4/M2 推进静态工具 argument-aware 执行：conversation/research registry 中的 todo、remember、propose_tool、summarize、verify_claim、Task、fetch_url、query rewrite、atomic retrieval 等工具可直接消费本次 tool-use arguments；`search_corpus(strategy=bm25/vector/hybrid)` 会把同一份 query/top_k 传给底层原子检索。工具/runtime/agent 相关测试与全量 `pytest -q` 通过 668 个测试。
- 2026-04-30 继续按 review §4/M2 拉直 search/web tool input 链路：`agent_search_papers`、`agent_search_evidence` 和 `agent_web_search` 支持显式 tool input 参数，research registry 将当前 `search_corpus/web_search` arguments 直接传入 handler，保留从 state 读取的兼容 fallback。search/runtime/agent 相关测试与全量 `pytest -q` 通过 668 个测试。
- 2026-04-30 继续按 review §4/M2 修补 next-action tool args 丢失：`AgentPlanner.choose_next_action` 现在会把运行中 tool-call planner 返回的 `tool_call_args` 合并回 `state["tool_inputs"]`，后续 executor 执行 fallback/next action 时能拿到结构化参数。planner/runtime/agent 相关测试与全量 `pytest -q` 通过 669 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 solver pipeline 与 Agent mixin 面：新增 `concept_definition_solver.py` 承接概念解释 LLM/fallback claim 生成，`ResearchAssistantAgentV4` 不再继承 `ConceptReasoningMixin`，旧 `agent_mixins.concept_reasoning` 仅保留 marker 兼容出口；新增概念定义 solver 单测。概念/solver/agent 相关测试与全量 `pytest -q` 通过 671 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 `EntityDefinitionMixin`：新增 `entity_definition_profiles.py` 统一承载实体解释 marker profiles，新增 `entity_type_inference.py` 承接实体类型 LLM classifier 与 fallback classifier，mixin 内 `_infer_entity_type` 收敛为依赖注入转发；新增实体类型推断单测。entity/agent/solver 相关测试与全量 `pytest -q` 通过 673 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 `EntityDefinitionMixin` 支持论文选择面：新增 `entity_supporting_paper_selector.py` 承接 candidate lookup、evidence grounding、LLM supporting-paper 选择、上下文过滤、噪声剪枝与定义分数，mixin 内相关私有方法收敛为薄兼容转发；新增 service 级回归测试覆盖定义源优先、LLM 证据选择和噪声剪枝。entity/agent/runtime/solver 相关测试与全量 `pytest -q` 通过 676 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄实体定义 compose 黑盒：新增 `entity_answer_composer.py` 承接实体定义 Markdown fallback、supporting lines、机制/流程/奖励 bullet、描述清洗和实体描述生成，`EntityDefinitionMixin` 缩到兼容转发层并保留 marker 旧导入路径；新增 service 级 answer composer 测试。entity/agent/solver 相关测试与全量 `pytest -q` 通过 679 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 `ClaimVerifierMixin` 底层 helper：新增 `claim_verification_helpers.py` 承接 verification goals、metric goal 判断、公式形态/target 对齐、target support、paper identity/initialism alias 匹配，mixin 内对应私有方法收敛为兼容转发；新增 helper 单测覆盖 modality goals、formula target 与 alias identity。verifier/agent/entity 相关测试与全量 `pytest -q` 通过 682 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 `ClaimVerifierMixin` 的 LLM verifier：新增 `llm_claim_verifier.py` 承接 schema-claim verifier、formula-claim verifier 与 verifier list coercion，mixin 只注入 clients 并保留兼容私有入口；新增 LLM verifier 单测覆盖 schema retry、formula retry/pass payload 映射。verifier/agent/solver 相关测试与全量 `pytest -q` 通过 685 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 `ClaimVerifierMixin` 的 per-claim deterministic verifier：新增 `claim_type_verifiers.py` 承接 origin/entity/followup/recommendation/topology/figure/metric/formula/concept/general claim 校验，mixin 只负责依赖注入与旧私有入口；新增 claim type verifier 单测覆盖 origin intro support、follow-up seed 排除和 formula invalid/misaligned retry。verifier/agent/solver 相关测试与全量 `pytest -q` 通过 688 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer library 黑盒：新增 `library_recommendation_helpers.py` 承接 library status/recommendation markers、列表/推荐意图、论文预览、推荐排序/去重、最近推荐记忆和作者拆分，`answer_composer.py` 保留兼容转发；新增 helper 单测覆盖 listing/recommendation、survey 排序、预览、recent memory 与去重。library/agent/tool-registry 相关测试与全量 `pytest -q` 通过 692 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer 的 library metadata SQL 工具逻辑：新增 `library_metadata_sql.py` 承接 SQL schema 描述、只读 SQL 校验、内存 SQLite 执行、row payload 转换和兜底回答格式化，`answer_composer.py` 仅保留 planner/LLM answer 注入与兼容转发；新增 SQL helper 单测覆盖非法 SQL 拒绝、查询截断和聚合/列表兜底回答。library/agent/tool-registry 相关测试与全量 `pytest -q` 通过 695 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer 的 library metadata row shaping：`library_metadata_sql.py` 接管 paper-doc metadata 行构造、paper_id 去重、Zotero collection/category fallback、searchable_text 聚合、PDF 标记与作者/tag/category 展开，`answer_composer.py` 只读取 collection paths 后委托 helper；新增 row shaping 单测覆盖去重、分类 fallback、全文检索文本和文件类型标记。library/agent/tool-registry 相关测试与全量 `pytest -q` 通过 696 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer 的 library recommendation 选择器：`library_recommendation_helpers.py` 接管 LLM 推荐重排 prompt/payload 映射、最近推荐避让和 fallback 多样化选择，`answer_composer.py` 只注入 clients/settings 并保留薄兼容入口；新增 helper 单测覆盖 LLM title 映射、reason fallback、prompt payload 与无 LLM 时的 diversified fallback。library/agent/tool-registry 相关测试与全量 `pytest -q` 通过 698 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer 的 library status 生成：`library_recommendation_helpers.py` 接管 paper metadata 去重、分类/年份/PDF 统计、文章预览和默认推荐 Markdown，`answer_composer.py` 只读取 collection paths 并委托 helper；新增 helper 单测覆盖去重、库统计、分类概览、预览区与默认推荐区。library/agent/tool-registry 相关测试与全量 `pytest -q` 通过 699 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer 的公式答案格式化：新增 `formula_answer_composer.py` 接管公式 Markdown 渲染、自动候选提示、变量/term 说明、公式符号格式化与 math artifact 归一化，`answer_composer.py` 仅保留兼容转发；公式 marker 从 answer composer marker 表迁出。新增公式 composer 单测覆盖 LaTeX 渲染、变量说明优先级、候选提示和 math artifact 修正。formula/agent 相关测试与全量 `pytest -q` 通过 702 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer 的 topology recommendation 格式化：`topology_recommendation_helpers.py` 接管 public text 清洗、负面/无证据英文抑制和最终 DAG 组织建议 Markdown，`answer_composer.py` 仅保留兼容转发；answer composer marker 表清空并迁入领域 helper。新增 topology helper 单测覆盖负面文本清洗和推荐答案 fallback。topology/agent 相关测试与全量 `pytest -q` 通过 703 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer 的 metric/paper summary 格式化：新增 `paper_answer_composer.py` 接管 metric lines 去重、metric value Markdown、paper summary result Markdown 和 core point fallback，`answer_composer.py` 仅保留兼容转发；新增 paper answer composer 单测覆盖指标行去重、原始表格行保留、核心结论抽取与 fallback 摘要。metric/paper/agent 相关测试与全量 `pytest -q` 通过 707 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 answer composer 的 follow-up research 格式化：新增 `followup_answer_composer.py` 接管后续工作分组、selected candidate verdict、关系证据展示和 public reason 清洗，`answer_composer.py` 仅保留兼容转发并降至 983 行；新增 follow-up answer composer 单测覆盖英文 reason 替换、直接/强相关分组和候选判断答案。follow-up/agent 相关测试与全量 `pytest -q` 通过 710 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 solver pipeline 的 entity definition 分支：新增 `entity_definition_claim_solver.py` 接管 supporting paper/evidence 到 entity claim 的构造、entity type 注入和 support lines 映射，`solver_pipeline.py` 只注入现有 selector/type/line 依赖并降至 314 行；新增 entity definition claim solver 单测覆盖选中论文 claim 构造和无 supporting paper 空结果。entity/solver/agent 相关测试与全量 `pytest -q` 通过 712 个测试。
- 2026-05-01 继续按 review §10/M3 拆薄 claim verifier 分发：新增 `claim_verifier_pipeline.py` 接管 verification goals 到 verifier check 的顺序选择和 generic fallback 执行，`ClaimVerifierMixin` 只注册现有 verifier 方法；新增 verifier pipeline 单测覆盖 origin/entity/metric/formula 顺序、reward/general fallback 和未知 goal 默认 general。verifier/agent 相关测试与全量 `pytest -q` 通过 715 个测试。
- 2026-05-01 继续按 review §7/M1 收口置信度阈值配置：`intent_router.query_contract_from_router_decision` 不再硬编码低置信阈值 `0.6`，改为接收 `confidence_floor`，contract extraction 从 `AgentSettings` 注入；新增 router 单测覆盖 0.65 置信度在 0.6/0.7 floor 下分别进入研究路径或澄清路径。intent/router/agent 相关测试与全量 `pytest -q` 通过 716 个测试。
- 2026-05-01 按 review 当前落地状态同步 checklist：M1 已全部完成，M2/M3 标注已完成的 tool-use/schema/confidence/dynamic-tool/learnings/trace-diff 部分，并明确剩余待收口项为 `intent.py` fallback markers、LLM-judge eval 与 solver pipeline 兼容 `_solve_*` 入口删除；全量 `pytest -q` 通过 716 个测试。
- 2026-05-01 继续按 review §12/M3 接入 LLM-judge eval：新增 `eval_judge.py`，把 eval case/query/answer/expectations 与 `TraceDiff` 稳定签名打包给 LLM 裁判，并规范解析 `pass/fail/needs_review`、score、missing 与 unsupported；新增 eval judge 单测覆盖 prompt payload、LLM payload coercion、无 chat 模型 unavailable 和 score 边界。eval/trace 相关测试与全量 `pytest -q` 通过 720 个测试。
- 2026-05-01 继续按 review §10/M3 收口 solver 兼容层：deterministic solver fallback 从“stage -> 字符串方法名 -> `getattr(runner, _solve_*)`”改为显式 `DeterministicSolverHandlers` 注入，运行主路径不再依赖 `SolverPipelineMixin` 的 `_solve_*` 方法名；`solver_pipeline.py` 直接构造服务级 handler，为后续删除兼容 `_solve_*` 入口铺路。deterministic/solver 相关测试与全量 `pytest -q` 通过 720 个测试。
- 2026-05-01 继续按 review §10/M3 删除无调用点 solver 兼容入口：移除 `SolverPipelineMixin` 中 origin/entity/concept/topology/paper-summary/paper-recommendation/metric/default 等 `_solve_*_text` 转发方法，deterministic 主路径继续走显式 handler，`solver_pipeline.py` 降至 282 行；剩余待迁移入口集中为 follow-up/formula/table/figure 的测试兼容方法。solver/agent 相关测试与全量 `pytest -q` 通过 720 个测试。
- 2026-05-01 完成 review M3 solver pipeline 兼容入口收口：`tests/test_agent_v4.py` 中专用 solver 回归迁到 `formula/table/figure/followup` service 级入口，`SolverPipelineMixin` 删除剩余 `_solve_followup_research/_solve_formula/_solve_table/_solve_figure`，并将 schema/deterministic 分支入口改名为 `_run_*_claim_solver`；`solver_pipeline.py` 已无 `def _solve_*`，降至 229 行。solver/agent 相关测试与全量 `pytest -q` 通过 720 个测试。
- 2026-05-01 完成 review M2 intent marker 收口：原 `intent.py` 已不存在，research/library/follow-up 中承接 H1-H10 的兼容 marker 不再以 Python 大数组散落，迁入 `app/services/intent_marker_profiles.json` 并通过 `intent_marker_matching.marker_profile(_map)` 统一加载；新增 profile loader 回归测试，确保 origin/library/citation/formula-correction 等兜底仍可用。intent/router/agent 相关测试与全量 `pytest -q` 通过 723 个测试。
- 2026-05-01 修复 V5 实测暴露的过度澄清与证据可见性问题：LLM router 即使误判为 `need_clarify`、低置信或 direct answer，只要问题中已有明确研究对象和公式/figure/总结/对比等研究槽位，就恢复为 research contract 进入工具循环；覆盖 DPO 公式、AlignX 结论数据支持、DeepSeek figure、GRPO/PPO/DPO 对比等回归。研究回答在存在 citations 时会自动追加可见“引用来源”段落，V5 Run 面板从线性卡片改为 Mermaid-like 流程图，展示 intent->plan->tool loop->evidence->claims->verification->answer 以及工具循环轮次。相关测试与全量 `pytest -q` 通过 728 个测试。
- 2026-05-01 收口 V5 复合任务规划架构：删除 `compound_intents.py` 及其 marker-gate 测试，复合任务是否拆分不再由硬编码关键词预筛决定，而是每轮统一调用 LLM decomposition planner，由 planner 输出 `is_compound/subtasks` 后再经 QueryContract schema/normalization 校验；单任务返回 false 后继续标准 agent loop。撤回后端强插“引用来源”正文段落，继续使用前端 citation chips/evidence board 展示来源；claim verification 对多 targets 改为要求每个 target 都被证据覆盖，避免 DPO 证据支撑 GRPO/PPO/DPO 整体比较。相关测试与全量 `pytest -q` 通过 723 个测试。
- 2026-05-01 修复复合任务澄清后的执行断裂：compound 子任务触发消歧时会把完整 subtask plan 写入 session working memory；用户选择含义后先解析 pending clarification，再用选中 contract 替换 blocked subtask 并恢复原 compound plan 继续执行后续检索与 comparison，避免 GRPO 消歧后只回答 GRPO 定义而丢失 PPO/DPO 比较。新增回归覆盖 pending compound plan 恢复、comparison_synthesis 继续执行和 pending 清理；相关测试与全量 `pytest -q` 通过 723 个测试。
- 2026-05-01 修复 V5 实测暴露的 recovered research 过早澄清：planner/runtime 对已归一化为 research 且带有 targets、answer slots、follow-up 上下文或 recovered research note 的合约，会先进入读记忆/检索工具循环，不再被旧的 `need_clarify`、`low_intent_confidence` note 抢先打断；若初始 tool planner 或 next-action planner 仍返回 `ask_human`，也会延迟到检索观察后再处理。覆盖 AlignX 结论/数据支持类查询的 planner、runtime 和 AgentPlanner 回归；相关测试与全量 `pytest -q` 通过 726 个测试。
- 2026-05-01 服务层第一阶段分包收口：`app/services` 根目录只保留 package 入口，agent/runtime/tool-loop、intents、contracts、planning、claims、answers、retrieval、library、memory、tools、infra 等按职责迁入子包；保留 `app.services.agent/library/retrieval` 公共 re-export 入口，避免依赖注入层感知内部 core 文件。同步迁移 `intent_marker_profiles.json` 到 `intents` 包，更新代码、脚本和测试 import 路径；`compileall`、架构边界测试、核心 agent 测试与全量 `pytest -q` 通过 726 个测试。
